# APPLE.md — App Store 출시 계획 (2단계)

**작성**: 2026-09-05 · 4.3(b) 3연속 거절(6/8 · 8/13 · 9/3) 이후.
편지 전문·검증 근거·데모 계정은 [`APPEAL.md`](APPEAL.md). 이 문서는 **앞으로 할 일**만 적는다.

## 0. 전략

| 단계 | 내용 | 심사 유형 |
| --- | --- | --- |
| **1차** | 측정 전용 빌드. 관상 서술을 iOS 빌드에서 **제거**하고 결과는 숫자(계측값·평균 대비 편차·점수)만 보여준다 | 새 제출 — 4.3(b) 를 정면으로 통과해야 한다 |
| **2차** | "해석 모드" 추가. 닮은꼴(기본) / 첫인상 지표 / 얼굴 균형 점수 / 관상 중 선택. 관상은 옵션 하나 | 업데이트 심사 — 4.3(b) 검사가 새 제출보다 약하다 |
| Android | 그대로 간다. 엔진·서버·계정은 iOS 와 공유 | — |

근거: 4.3(b) 명단은 dating · flashlight · sound effects · wallpaper · simple timers · fortune telling. 얼굴 계측·닮은꼴은 명단 밖이고, 2025~2026 년에 개인 개발자의 새 닮은꼴·얼굴 점수 앱이 승인됐다(`APPEAL.md` 근거 표 참조). 세 통의 편지가 문제 삼은 단어는 **primarily** 다. 1차는 관상을 없애서, 2차는 관상을 옵션으로 내려서 그 단어를 피한다.

이름 **관상은 과학이다** 는 유지한다. 화면이 측정 도구로 읽히면 이름은 브랜드로 남는다.

## 1. 3차 편지의 세 항목 — 현재 상태

| 항목 | 상태 | 남은 일 |
| --- | --- | --- |
| Guideline 4 — 로그인이 Safari 앱으로 나감 | 코드 수정 완료 (`auth_service.dart` iOS 는 in-app browser, 세션 도착 시 시트 닫음) | 카카오톡 없는 iPhone 실기기 확인. 답변에 "수정됨 + 확인 경로" 한 문단 |
| 2.1 — 얼굴 데이터 설명 요구 | `web/public/privacy.md` 제2조(한글 2.1~2.5 + 영문 2.6) 작성 완료 | web 빌드·배포(`wrangler deploy` 전 build 필수). 답변에 제2조 인용. App Store Connect 앱 개인정보 라벨에 얼굴 데이터 항목 확인 |
| 4.3(b) — 점술 앱 | 이번 계획의 본체 | §2 전부 |

## 2. 1차 빌드 — 측정 전용 (edition: `measure`)

### 2.1 빌드 구분 방식

- `--dart-define=FACELY_EDITION=measure` 를 `const String.fromEnvironment` 로 읽는 **컴파일 상수**. iOS 빌드는 항상 이 값으로 만든다. Android 는 `full`.
- 관상 서술을 그리는 위젯·조립 코드는 `const` 분기 뒤에 두어 release 빌드에서 **tree-shake 로 사라지게** 한다. 원격 설정(`app_config.ios_narrative_version`)이나 런타임 플래그로 숨기지 않는다. 가이드라인 2.3.1(숨겨진·잠자는 기능 금지)에 걸리면 앱 거절이 아니라 계정 문제가 된다.
- 지금 있는 `app_config.ios_narrative_version` 원격 스위치는 measure 에디션에서 **읽을 코드 자체가 없어야** 한다. 2차에서 관상 모드가 정식 코드로 들어갈 때 다시 쓴다.
- 엔진(`shared/`)은 손대지 않는다. 서술 생성 함수는 남아 있어도 iOS 화면이 호출하지 않으면 빌드에서 빠진다.

### 2.2 화면별 작업

리뷰어가 4.3(b) 근거로 찍은 화면은 **온보딩 삽화 3장 + 관상 탭 빈 화면** 이었다. 서술만 빼면 안 되고 이 화면들이 먼저 바뀌어야 한다.

| 영역 | 지금 | 1차 | 파일 |
| --- | --- | --- | --- |
| 온보딩 삽화 | 도사·족자·붓 그림 4장 | 계측 오버레이 실제 화면 캡처 기반 이미지 4장. 도사·족자 0장 | `assets/images/onboarding0~4.png`, `onboarding_intro.dart` |
| 온보딩 제목·본문 | "관상으로 풀어보는 친구 만들기" / "관상" / "궁합" / "케미" | "얼굴을 재서 케미를 계산합니다" / "측정" / "비교" / "케미" — 탭 이름과 같은 단어. 본문에서 관상·궁합·해석 단어 제거 | `onboarding_intro.dart::_kPages` |
| 탭 이름 (**확정**) | 관상 · 궁합 · 케미 · 채팅 · 설정 | 측정 · 비교 · 케미 · 채팅 · 설정. 이름만 교체 — 5탭·IndexedStack·인원수 순서·아이콘·시작 탭(케미) 그대로. 각 화면 앱바 제목과 온보딩 2·3장 제목도 같은 단어 | `app.dart` BottomNavigationBarItem, 각 화면 AppBar title |
| 측정 탭 빈 화면 문구 | — | "얼굴을 등록하면 28개 계측값과 평균 대비 편차를 보여줍니다." | `physiognomy_screen.dart` |
| 비교 탭 빈 화면 문구 | — | "내 얼굴을 등록한 뒤 올리는 사진 속 얼굴과 계측값을 비교합니다." | `compatibility_screen.dart` |
| 관상 탭 빈 화면 | "관상은 미래의 운명을 단정짓는 점술이 아니라 … 전통적 오랜 지혜" | 삭제. "얼굴을 등록하면 28개 계측값과 평균 대비 편차를 보여줍니다" 두 줄 | `physiognomy_screen.dart::_kCreditsIntro`, `my_face_header.dart` 점술가 일러스트 |
| 카메라·앨범 캡처 | 계측 오버레이(정면 28 · 측면 8), 각도 상태 pill | 완료. 그대로 스토어 스크린샷 1장으로 쓴다 | `face_metric_overlay_painter.dart` 외 |
| 관상 리포트 | archetype 카드 + 10 속성 + **관상 해석 본문** + 14 노드 점수(십이궁 이름) + 근거 | archetype 카드·해석 본문·노드 점수 섹션 **제거**. 속성 10 은 이름이 성격 서술(리더십·관능 등)이라 **제거**. 남기는 것과 새 형식은 §2.2.1 (백분위·희귀도 리포트) | `report_page.dart::_buildArchetypeCard / _buildReadingSection / _buildNodeScoreSection / _buildAttributeSection` |
| 궁합 상세 | 4단 라벨 · 관계평 · 오행평 · 해시태그 | 종합 점수 하나 + 가장 닮은 지표 3 / 가장 다른 지표 3 (§2.2.1). 라벨·평·해시태그 **제거** | `compatibility_detail_screen.dart`, `compat_hashtags.dart` 호출부 |
| 궁합 설명 | "십이궁 40% · 오행 20% …" 본문 | 제거. "두 얼굴의 계측값이 얼마나 가까운지로 점수를 냅니다" 한 줄 | `compatibility_screen.dart` |
| 케미 그룹 | N×N 매트릭스 · 베스트 쌍 · 채팅 · 해시태그 | 매트릭스·점수·순위·베스트 쌍·채팅 유지. 해시태그는 §4 결정 | `chemistry/*`, `team_*` |
| 설정 | narrative 관련 항목 | measure 에디션에서 제거 | `settings_screen.dart:175` 부근 |
| 공유 카드 이미지 | 관상 문구 포함 | 계측 요약 카드로 | `share_publisher.dart` |
| 웹 리포트 `/r/{id}` | 서술 포함 SSR | §4 결정 (iOS 공유 링크에서 열림) | `web/app/routes/r.$id.tsx` |

#### 2.2.1 결과 화면 형식 — 백분위·희귀도 리포트 (확정)

서술을 빼고도 흥미를 지키는 재료는 이미 있다: AAF 11,800 실측 레퍼런스와 그걸로 나오는 z-score. "내가 평균에서 어디쯤인지"를 보여주는 형식으로 간다. 형용사는 0개.

| 화면 | 형식 |
| --- | --- |
| 측정 리포트 | 첫 장 = 카메라 오버레이 스냅샷. **내 얼굴의 특이점 Top 3** 카드(z-score 절댓값 상위 3) + **평균에 가장 가까운 지표 3** 카드. 그 아래 28개 계측 표 — 값 · 한국인 상위 N% · 편차 막대. 얼굴형 5형 확률 막대. 측면 8개도 같은 표 |
| 비교 상세 | 종합 점수 하나(궁합 엔진 점수 그대로, 오행·십이궁 이름은 표시 안 함) + **가장 닮은 지표 3 / 가장 다른 지표 3** — 두 사람 값 나란히 |
| 공유 카드 | "내 얼굴 특이점 Top 3" 한 장. 희귀도("상위 3%")가 공유 동기 |
| 케미 | 매트릭스·순위·베스트 쌍 그대로 |

- 구현: 백분위 변환 함수 하나(z → 상위 %) + 리포트 화면 재구성. 엔진 변경 없음.
- 여기서 나오는 지표별 백분위·차이가 2차 닮은꼴 점수와 첫인상 지표의 재료가 된다.

**검토했다가 2차로 미룬 안 — 첫인상 지표.** 28개 계측을 지각 심리학의 인상 축(친근함·신뢰감·존재감·어려 보임·또렷함) 4~5개로 묶어 점수화하고 각 축 아래 근거 계측 3개를 붙이는 형식. 성격·운이 아니라 "남이 받는 인상"이고 문헌 근거가 있어 fortune telling 으로는 안 읽히지만(스토어의 Face Yourself 가 이 형식), 형용사가 들어가 1차 "정량만" 원칙에서 반 발짝 나간다. 지금 10 속성 파생 로직이 구조상 이것과 같아 속성 이름·가중치 재정의로 만들 수 있다. → §3 해석 모드로.

### 2.3 스토어 메타데이터

| 항목 | 1차 |
| --- | --- |
| 이름 | 관상은 과학이다 (유지) |
| 부제 | 얼굴 계측으로 보는 우리 그룹 케미 |
| 설명 | 첫 문단에 468점 · 28 계측 · 평균 대비 편차 · N×N 케미. 관상·운·점·해석 단어 0회 |
| 키워드 | 얼굴 측정, 얼굴 비율, 닮은꼴, 케미, 그룹 |
| 스크린샷 | 1장 = 카메라 계측 오버레이(정면). 2장 = 측면 오버레이. 3장 = 케미 매트릭스. 4장 = 계측 표. 5장 = 방 초대 |
| 카테고리 | §4 결정 |
| 리뷰 노트 | 로그인 없이 1분 동선(케미 목록 → 방 → 매트릭스) + 데모 계정 동선. "This is a facial measurement and group compatibility app. It does not read fortunes." 첫 줄 |
| 답변 (Resolution Center) | 4.3(b): 컨셉 변경 설명 3문단 · Guideline 4: 수정 완료 · 2.1: privacy.md 제2조 인용 |

### 2.4 온보딩 이미지 추천안 (iOS 1차)

**규격**: 1200×752 PNG, `BoxFit.contain` (지금과 같다). 글자는 이미지에 굽지 않는다 — 제목·본문은 위젯이 그린다.
**금지 요소**: 도사·갓·족자·붓·향로·고서·한자 편액. 리뷰어가 fortune telling 근거로 찍은 게 정확히 이것들이다.
**유지 요소**: 세피아 선화 톤, 한 장에 인물 1~3명, 배경 여백. 지금 삽화와 같은 화풍이라 앱 전체 톤이 안 흔들린다.

두 가지 방향 중 **A 를 추천**한다.

| | A. 실제 화면 캡처 합성 | B. 삽화 전면 교체 |
| --- | --- | --- |
| 방법 | 세피아 선화 인물 + 손에 든 폰 화면 자리에 **실제 앱 캡처**(오버레이·매트릭스)를 합성 | 도사를 로봇 의사·계측 장비로 바꾼 새 삽화 4장 |
| 리뷰어 인상 | 스토어 스크린샷과 온보딩이 같은 화면을 보여줘 "측정 도구" 가 두 번 확인된다 | 캐릭터가 바뀌어도 "누군가 얼굴을 읽어 준다" 구도는 남는다 |
| 제작 | 인물 선화 4장 + 캡처 4장 합성. 캡처는 오늘 만든 오버레이가 그대로 재료 | 삽화 4장 새로 생성. 화풍 맞추기 반복 필요 |
| 유지보수 | 화면이 바뀌면 캡처만 갈아끼운다 | 삽화 재생성 |

**슬라이드별 구성 (A 기준)**

| # | 파일 | 장면 | 폰 화면에 넣을 캡처 | 제목 (위젯) | chip |
| --- | --- | --- | --- | --- | --- |
| 1 | onboarding1 | 친구 셋이 카페 테이블에 앉아 한 명이 폰을 들고 웃는다. 세피아 선화. 지금 1장의 3단 구성(친구→그룹→출구)은 유지하되 그림 속 "관상 3×3 매트릭스" 족자를 **케미 매트릭스 실제 캡처**로 | 케미 방 매트릭스(8인 28쌍) | 얼굴을 재서\n우리 그룹 케미를 봅니다 | — |
| 2 | onboarding2 | 한 사람이 정면으로 폰을 들고 셀피. 폰 화면이 크게 | **정면 계측 오버레이** (28 라벨, 녹색 상태, "측정에 적합한 각도입니다" pill) | 측정 | 무료 |
| 3 | onboarding3 | 두 사람이 나란히, 사이에 폰 하나 | **두 얼굴 계측 차이 표** (1차 궁합 상세 화면) 또는 측면 오버레이 | 비교 | 1코인 |
| 4 | onboarding4 | 초대 링크를 받는 장면 — 카톡 말풍선 안에 초대 카드, 옆에 QR | 방 상세(참가자 아바타 8 + "결과 공개까지 2명") | 케미 | 무료 |

- 2장이 핵심이다. 리뷰어가 온보딩에서 처음 보는 "얼굴" 화면이 도사가 아니라 자와 숫자여야 한다.
- 3장의 측면 오버레이(귀→턱 선 + E라인)는 "의료 계측" 인상이 강해서, 두 얼굴 비교 표가 준비 안 되면 이걸 쓴다.
- 캡처는 데모 계정 얼굴이 아니라 **선화 얼굴을 다시 찍은 것**을 쓴다. 실제 인물 얼굴을 온보딩에 굽지 않는다(얼굴 데이터 조항과 어긋난다).
- B 를 고른다면 캐릭터는 로봇 의사보다 **흰 가운 없는 계측 기사**(줄자·캘리퍼)가 낫다. 의사 캐릭터는 5.1.1 의료 주장 오해를 부른다.

### 2.5 제출 전 확인

- [ ] 실기기: 카카오 로그인 시트가 앱 안에서 뜨고 로그인 뒤 닫힌다 (카카오톡 미설치 iPhone)
- [ ] 실기기: 정면·측면 오버레이 라벨 열이 화면 안에 들어온다
- [ ] iOS release 빌드에 관상 서술 문자열이 없다 — `strings Runner.app/Frameworks/App.framework/App | grep -c '재물운'` 같은 검사로 0 확인
- [ ] web 빌드·배포 후 `https://facely.kr/privacy.md` 제2조 확인
- [ ] App Store Connect 앱 개인정보 라벨: 얼굴 데이터 = "사용자에게 연결됨" 항목 확인
- [ ] 데모 방 seed 실행 (`web/db/tests/demo_teams.sql` — `delete from public.teams` 로 시작하므로 실행 시점의 모든 방이 사라진다)
- [ ] 스크린샷 6.9" 1320×2868 5장 교체
- [ ] `flutter analyze` 0 · `flutter test` green

## 3. 2차 빌드 — 해석 모드

정식 업데이트로 제출한다. 1차가 스토어에 있는 상태라 거절돼도 1차는 남는다.

| 모드 | 내용 | 구현 |
| --- | --- | --- |
| 닮은꼴 (기본값) | 두 얼굴 계측 벡터 거리 → 0~100 유사도. 케미 점수에 가산 | `shared/` 에 similarity 함수 하나. 정면 26 z-score 벡터의 거리. 케미 쌍 점수 식에 항 추가 |
| 첫인상 지표 | 인상 축 4~5개(친근함·신뢰감·존재감·어려 보임·또렷함) 점수 + 축별 근거 계측 3개. 비교는 두 사람 레이더 겹침(비슷한 인상 / 보완하는 인상) | 10 속성 파생 로직을 인상 축으로 재정의(이름·가중치를 문헌 기준으로). 공유 카드 = 레이더 + 최고 축 이름 |
| 얼굴 균형 점수 | 대칭·비율·E라인 기반 균형 점수. "PSL" 단어는 쓰지 않는다 | 계측값의 평균 편차 합성. 등급 아닌 점수만 |
| 관상 | 지금 Android 가 쓰는 서술 그대로 | measure 에디션에 서술 위젯을 **정식 코드로** 되살린다. 모드 선택 UI 뒤, 기본 꺼짐 |

- 모드 선택은 설정 화면의 한 항목. 결과 화면 상단 chip 으로도 전환.
- 스토어 설명에 "해석 모드 4종" 을 명시. 숨기지 않는다.
- 관상 모드가 거절되면 그 모드만 빼고 나머지 둘로 업데이트를 다시 낸다.

## 4. 결정이 필요한 항목

| 항목 | A | B | 추천 |
| --- | --- | --- | --- |
| 앱 레코드 | 지금 레코드(거절 3회 이력)에 2.1.0 으로 제출 | 새 번들 ID·새 레코드 | **A**. 편지에 "concept reconsidered" 를 적고 같은 레코드로. 새 레코드는 이력을 지우지만 리뷰 계정·구매·리뷰 노트를 다시 세팅해야 하고, 같은 개발자 계정이라 이력 은폐도 안 된다 |
| 카테고리 | 소셜 네트워킹 (지금) | 라이프스타일 | **B**. 승인된 얼굴 계측·닮은꼴 앱 대부분이 Lifestyle / Photo & Video. 리뷰어가 같은 칸의 앱들과 나란히 본다 |
| 케미 해시태그 (#케미 #호흡척척 …) | 유지 | 1차에서 제거, 2차 닮은꼴 모드에서 복귀 | **B**. 1차는 "숫자만" 원칙을 예외 없이 |
| 웹 리포트 `/r/{id}` 서술 | 유지 (Apple 심사 대상 아님) | iOS 에서 만든 카드는 서술 없는 렌더 | **A**. 심사 대상이 아니고 Android 사용자 카드와 같은 링크 형식이어야 한다. 리뷰 노트에 링크를 넣지 않는다 |
| 궁합 1코인 정책 | 유지 | 1차 무료 | **A**. 결제는 4.3 과 무관 |

## 5. Android

- 변경 없음. edition `full` 로 계속 빌드.
- 같은 방에 iOS·Android 사용자가 섞여도 점수는 같은 엔진이라 동일하다. 다른 건 화면의 서술 유무뿐.
- 스토어 문구는 두 벌이 된다. Play 는 지금 문구 유지.
