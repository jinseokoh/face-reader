# 얼굴 분석 앱

## 정량적 얼굴 기하학 및 첫인상 분석 시스템 요구사항

**문서 버전:** 1.0
**작성 목적:** 기존 관상 중심 앱을 정량적 얼굴 기하학·첫인상 분석·두 얼굴 비교 중심의 제품으로 전환하기 위한 시스템 요구사항 정의
**주요 구현 대상:** Claude Code 기반 개발 프로젝트
**주요 사용자:** 한국 사용자
**서비스 언어:** 한국어

---

# 1. 제품 개요

## 1.1 제품의 새로운 정의

본 서비스는 사용자의 얼굴 사진을 입력받아 얼굴의 **기하학적 구조를 정량적으로 측정**하고, 해당 얼굴이 사람들에게 형성할 가능성이 있는 **첫인상 특성을 통계적·기계학습적으로 추정**하며, 두 사람의 얼굴을 비교하여 **닮은 정도·조화도·보완도**를 분석하는 얼굴 분석 서비스이다.

본 서비스는 관상, 사주, 운세, 점술 또는 실제 성격 판정을 목적으로 하지 않는다.

서비스가 분석하는 대상은 다음과 같이 엄격하게 정의한다.

> **사람 그 자체가 아니라 얼굴이라는 시각적 정보가 만들어내는 측정값과 지각된 첫인상이다.**

---

# 2. 반드시 폐기해야 하는 기존 제품 논리

기존 관상 앱에서 사용하던 다음 논리는 제품의 핵심 기능에서 제거한다.

## 2.1 금지되는 분석

다음과 같은 형태의 결과를 제공하지 않는다.

- 얼굴 → 실제 성격
- 얼굴 → 운명
- 얼굴 → 재물운
- 얼굴 → 연애운
- 얼굴 → 건강 상태
- 얼굴 → 범죄 성향
- 얼굴 → 도덕성
- 얼굴 → 실제 신뢰성
- 얼굴 → 실제 지능
- 얼굴 → 실제 능력
- 얼굴 → 실제 연애 성공 가능성
- 두 얼굴 → 실제 연애 궁합
- 얼굴 → 미래 예측

예:

> "당신은 원래 성격이 강합니다."

사용하지 않는다.

> "당신의 관상은 재물운이 좋습니다."

사용하지 않는다.

> "두 분의 연애 궁합은 87점입니다."

사용하지 않는다.

---

# 3. 새로운 핵심 논리

서비스의 분석 흐름은 다음과 같다.

```text
얼굴 사진
    ↓
얼굴 검출
    ↓
468개 얼굴 랜드마크 추출
    ↓
얼굴 자세·위치·크기 정규화
    ↓
얼굴 기하학적 특징 추출
    ↓
얼굴 기하학 프로필
    ↓
첫인상 추정 모델
    ↓
첫인상 프로필
    ↓
사용자에게 결과 제공
```

두 사람 분석에서는:

```text
얼굴 A ─→ 기하학 프로필 ─┐
                          ├→ 두 얼굴 비교
얼굴 B ─→ 기하학 프로필 ─┘

얼굴 A ─→ 첫인상 프로필 ─┐
                          ├→ 첫인상 비교
얼굴 B ─→ 첫인상 프로필 ─┘
```

최종적으로:

```text
닮은 정도
조화도
보완도
```

를 별개의 지표로 제공한다.

---

# 4. 제품의 핵심 개념

## 4.1 얼굴 기하학

얼굴 이미지에서 추출된 468개 얼굴 랜드마크를 이용하여 얼굴의 형태와 비율을 정량화한다.

기본 데이터:

```text
468 landmarks
각 landmark:
- x
- y
- z
```

따라서 원시 데이터는 최대:

```text
468 × 3 = 1,404개 좌표값
```

으로 구성된다.

단, 원시 좌표를 그대로 사용자 점수에 사용하지 않는다.

---

# 5. 얼굴 기하학 정규화

얼굴 사진의 다음 요소는 가능한 한 제거한다.

- 사진 내 위치
- 얼굴 크기
- 회전
- 카메라에 대한 단순 자세 차이

이를 위해 얼굴 랜드마크를 정규화한다.

권장 처리:

```text
Raw landmarks
    ↓
Translation normalization
    ↓
Scale normalization
    ↓
Rotation / pose normalization
    ↓
Procrustes alignment
    ↓
Normalized facial geometry
```

목표는:

> "사진이 어디에 찍혔는가"가 아니라 "얼굴의 형태가 어떠한가"를 비교하는 것이다.

---

# 6. 얼굴 기하학 특징량

468개 점 전체를 동일한 의미의 독립적인 변수로 취급하지 않는다.

다음과 같은 파생 특징을 구성한다.

## 6.1 얼굴 전체

- 얼굴 가로 길이
- 얼굴 세로 길이
- 가로/세로 비율
- 얼굴 윤곽 비율
- 좌우 대칭성
- 얼굴 면적
- 얼굴 윤곽의 곡률
- 턱선 구조
- 하안면 비율

## 6.2 눈 영역

- 양 눈 사이 거리
- 양 눈의 상대적 크기
- 눈 가로 길이
- 눈 세로 개방도
- 눈꼬리 각도
- 눈 영역의 좌우 대칭성
- 눈과 눈썹 사이 거리

## 6.3 코 영역

- 코 길이
- 코 폭
- 코 폭/길이 비율
- 코와 얼굴 폭의 관계
- 코 중심축
- 코 영역 좌우 대칭성

## 6.4 입 영역

- 입 가로 길이
- 입 세로 비율
- 입과 얼굴 폭의 관계
- 입꼬리 각도
- 입 영역 좌우 대칭성

## 6.5 중안부 및 하안부

- 눈-코 거리
- 코-입 거리
- 입-턱 거리
- 얼굴 상·중·하부 비율
- 중안부 비율
- 하안면 비율

---

# 7. 얼굴 기하학 프로필

원시 좌표를 그대로 보여주지 않고 사용자가 이해할 수 있는 상위 지표로 변환한다.

예:

```text
얼굴 대칭성       82
얼굴 비율         74
얼굴 윤곽         68
눈 영역 구조      81
중안부 비율       71
하안부 구조       63
```

이 점수는 "사람의 가치"나 "사람의 매력"을 직접 평가하는 것이 아니라 해당 기하학적 특성이 기준 집단과 비교했을 때 어느 정도 위치에 있는지를 나타내는 방식으로 설계한다.

---

# 8. 기준 집단과 백분위

가능하면 단순한 임의 점수보다 **백분위(percentile)** 개념을 사용한다.

예:

```text
얼굴 대칭성
82점
상위 18%
```

단, 기준 데이터가 충분하지 않은 상태에서는 백분위를 임의로 생성하지 않는다.

기준 집단은 향후 데이터가 확보되면 다음을 고려한다.

- 성별
- 연령대
- 촬영 조건
- 이미지 품질
- 얼굴 자세

단, 성별·연령 등을 사용자의 가치 판단이나 차별적 평가에 사용하지 않는다.

---

# 9. 첫인상 분석

## 9.1 첫인상의 정의

서비스에서 말하는 첫인상은:

> 얼굴을 본 사람이 형성할 가능성이 있는 지각적 인상

으로 정의한다.

실제 사람의 성격이나 실제 능력을 의미하지 않는다.

---

# 10. 첫인상 분석 항목

초기 핵심 항목:

- 신뢰감 있는 인상
- 친근한 인상
- 따뜻한 인상
- 주도적인 인상
- 활기찬 인상
- 진지한 인상
- 매력적인 인상

필요에 따라 향후:

- 위협적으로 느껴지는 정도
- 편안한 인상
- 젊어 보이는 인상
- 건강해 보이는 인상

등을 연구·검증 후 추가할 수 있다.

단, 모든 항목은 반드시 **"실제 특성"이 아닌 "인상"**으로 표현한다.

---

# 11. 첫인상 점수의 언어 규칙

## 금지

```text
당신의 신뢰도는 82점입니다.
```

## 허용

```text
신뢰감 있는 인상
82점
```

또는:

```text
이 사진은 상대방에게 신뢰감 있는 인상을 줄 가능성이 높은 편입니다.
```

또는:

```text
이 얼굴은 일반적인 평가 상황에서 비교적 신뢰감 있게 인식되는 경향을 보입니다.
```

---

# 12. 첫인상 결과 화면

예시:

```text
첫인상 분석

신뢰감 있는 인상      82
친근한 인상            87
따뜻한 인상            78
주도적인 인상          41
활기찬 인상            67
진지한 인상            68
매력적인 인상          81
```

각 점수는 가능하면:

```text
점수
+
상대적 위치
+
간단한 설명
```

으로 제공한다.

---

# 13. "왜 이런 결과가 나왔나요?" 기능

첫인상 분석의 가장 중요한 UX 중 하나이다.

사용자가 결과를 보고:

> 왜 내가 친근하게 보인다는 거지?

라고 질문할 수 있어야 한다.

이때 모델이 임의의 심리학적 설명을 생성해서는 안 된다.

실제 계산에 사용된 얼굴 특징과 연결하여 설명한다.

예:

```text
친근한 인상 87

주요 관련 특징

- 눈 영역의 개방도가 높은 편
- 입꼬리의 형태가 비교적 부드러운 편
- 얼굴 좌우 대칭성이 높은 편
- 눈썹과 눈 사이의 비율이 기준 범위에 위치
```

설명에는 반드시:

> "이러한 특징이 해당 인상 점수와 통계적으로 연결되어 있다"

라는 구조를 유지한다.

---

# 14. 불확실성 표시

모든 결과를 절대적인 사실처럼 표시하지 않는다.

가능하면:

```text
신뢰감 있는 인상
82

분석 확신도: 높음
```

또는:

```text
82 ± 6
```

과 같은 형태를 사용할 수 있다.

모델의 실제 검증 성능이 확보되지 않은 경우 "정확도 92%" 등의 수치를 임의로 표시하지 않는다.

---

# 15. 두 사람 분석

서비스의 두 번째 핵심 기능이다.

사용자가 얼굴 사진 두 장을 입력하면 두 얼굴의 관계를 분석한다.

핵심 결과:

```text
닮은 정도
조화도
보완도
```

---

# 16. 닮은 정도

## 정의

> 두 얼굴의 기하학적 구조가 얼마나 유사한지를 나타내는 지표

계산 대상:

- 얼굴 윤곽
- 얼굴 비율
- 눈 영역
- 코 영역
- 입 영역
- 턱 영역
- 전체 landmark 구조

예:

```text
얼굴 전체        73
눈 영역          77
코 영역          65
입 영역          79
턱선             63

종합 닮은 정도   73
```

여기서 "닮은 정도"는 순수한 얼굴 형태 비교이다.

---

# 17. 조화도

## 정의

> 두 얼굴의 형태와 첫인상 특성이 함께 놓였을 때 시각적으로 자연스럽고 균형 있게 느껴지는 정도

조화도는 단순히 "닮은 정도"와 동일하지 않아야 한다.

따라서:

```text
닮은 정도 ≠ 조화도
```

이다.

두 얼굴이 매우 닮았지만 특정 요소가 함께 배치되었을 때 균형이 낮을 수 있고, 반대로 서로 상당히 다르더라도 전체적인 시각적 조화가 높을 수 있다.

---

# 18. 보완도

## 18.1 용어

영어 `Complementarity`는 서비스에서는 **"보완도"**로 표현한다.

"상보성"은 내부 연구 문서에서는 사용할 수 있지만 일반 사용자 UI에서는 사용하지 않는다.

---

# 18.2 보완도의 정의

> 두 사람의 첫인상 프로필에서 서로 다른 특성이 나타나며, 그 차이가 함께 놓였을 때 서로 보완적인 패턴을 이루는 정도

중요:

**보완도는 실제 성격의 보완성을 의미하지 않는다.**

예:

```text
A

따뜻한 인상       높음
친근한 인상       높음
주도적인 인상     낮음


B

따뜻한 인상       낮음
친근한 인상       보통
주도적인 인상     높음
```

이 경우:

```text
첫인상 유사도      낮음
보완도             높음
```

이라는 결과를 만들 수 있다.

---

# 19. 보완도를 설명하는 사용자용 논리

사용자가:

> "왜 우리 둘의 보완도가 높은데?"

라고 물었을 때 다음 논리를 사용한다.

```text
사람의 얼굴은 하나의 인상만 만드는 것이 아니라
따뜻함, 친근함, 주도성, 활력 등 여러 인상 차원을
동시에 만들어냅니다.

두 사람의 첫인상 프로필을 비교하면
어떤 부분은 비슷하고 어떤 부분은 서로 다를 수 있습니다.

보완도는 이러한 차이가 서로 다른 방향으로 나타나는 정도를
분석한 지표입니다.
```

핵심은:

> "서로 다른 성격이라서 잘 맞는다"

가 아니다.

대신:

> "두 얼굴에서 형성되는 첫인상 프로필이 어떤 부분에서 서로 다른 방향을 보이는가"

를 측정한다.

---

# 20. 보완도에 대한 제품상의 표현

예:

```text
보완도 84

두 분은 첫인상에서 차이를 보이는 영역이 많습니다.

한쪽은 상대적으로 따뜻하고 친근한 인상을,
다른 한쪽은 상대적으로 주도적이고 활기찬 인상을
형성하는 경향이 있습니다.

따라서 두 사람의 첫인상 프로필은
서로 다른 특성이 나타나는 보완적인 형태에 가깝습니다.
```

단:

```text
그래서 연애가 잘 됩니다.
```

라고 결론내리지 않는다.

---

# 21. 조화도와 보완도의 관계

둘은 반대 개념이 아니다.

예:

```text
닮은 정도       72
조화도           86
보완도           81
```

이런 결과가 가능하다.

의미:

> 어느 정도 닮았으며, 전체적으로 함께 놓였을 때 조화가 높고, 동시에 첫인상 특성에서는 서로 다른 부분이 있어 보완적인 패턴도 나타난다.

---

# 22. 두 사람 결과 화면

권장 UI:

```text
FACE CHEMISTRY

          A          B

얼굴 형태   ───────────

닮은 정도
███████████████░░░ 73

첫인상
████████████████░░ 81

조화도
█████████████████░ 86

보완도
████████████████░░ 81
```

단, 한국어 UI에서는 제목을:

> **두 얼굴 분석**

또는

> **얼굴 케미 분석**

등으로 검토한다.

영어 명칭은 내부 코드에서만 사용해도 된다.

---

# 23. 두 사람 분석의 세부 결과

"자세히 보기"에서 다음을 제공한다.

## 얼굴 형태

```text
얼굴 윤곽        72
눈 영역          77
코 영역          65
입 영역          79
턱선             63
```

## 첫인상

```text
신뢰감           A 82 / B 71
친근함           A 87 / B 73
따뜻함           A 78 / B 61
주도성           A 41 / B 78
활력             A 67 / B 84
```

## 관계 지표

```text
닮은 정도        73
조화도            86
보완도            81
```

---

# 24. 얼굴 중첩 시각화

두 얼굴을 비교할 때 단순한 숫자만 보여주지 않는다.

가능하면:

```text
Face A
+
Face B
+
Normalized landmark overlay
```

를 시각적으로 제공한다.

사용자가:

> "우리가 어디가 닮았지?"

라고 바로 이해할 수 있어야 한다.

예:

- 눈 영역 강조
- 코 영역 강조
- 입 영역 강조
- 얼굴 윤곽 강조

---

# 25. 얼굴 형태 차이 시각화

예:

```text
닮은 부분

눈 영역       매우 유사
입 영역       유사

다른 부분

턱선          차이가 큼
코 영역       차이가 있음
```

사용자는 숫자를 보지 않아도 두 얼굴의 차이를 이해할 수 있어야 한다.

---

# 26. 얼굴 기하학 데이터 모델

권장 내부 구조:

```text
FaceAnalysis
├── image_metadata
├── landmarks
├── normalized_landmarks
├── geometric_features
├── geometry_profile
├── impression_profile
├── model_version
├── confidence
└── created_at
```

---

# 27. 원시 랜드마크 데이터

가능하면 원본과 정규화 데이터를 분리한다.

```json
{
  "landmarks": [
    {
      "index": 0,
      "x": 0.123,
      "y": 0.456,
      "z": -0.032
    }
  ]
}
```

정규화:

```json
{
  "normalized_landmarks": [
    {
      "index": 0,
      "x": 0.021,
      "y": -0.114,
      "z": 0.008
    }
  ]
}
```

---

# 28. 기하학 특징 데이터

예:

```json
{
  "face_width_height_ratio": 0.82,
  "facial_symmetry": 0.91,
  "eye_distance_ratio": 0.31,
  "eye_openness": 0.74,
  "nose_width_ratio": 0.28,
  "nose_length_ratio": 0.34,
  "mouth_width_ratio": 0.42,
  "jaw_angle": 118.4
}
```

실제 feature 명칭과 계산식은 구현 전에 별도의 `GEOMETRY_FEATURES.md`에 정의한다.

---

# 29. 첫인상 데이터 구조

```json
{
  "trustworthiness_impression": 82,
  "approachability_impression": 87,
  "warmth_impression": 78,
  "dominance_impression": 41,
  "energy_impression": 67,
  "seriousness_impression": 68,
  "attractiveness_impression": 81
}
```

각 값은 반드시:

```text
score
model_version
confidence
```

를 함께 관리한다.

---

# 30. 두 얼굴 비교 데이터 구조

```json
{
  "facial_similarity": 73,
  "impression_similarity": 81,
  "harmony": 86,
  "complementarity": 81,
  "model_version": "..."
}
```

사용자에게 표시할 때는:

```text
facial_similarity
→ 닮은 정도

impression_similarity
→ 첫인상 유사도

harmony
→ 조화도

complementarity
→ 보완도
```

로 매핑한다.

---

# 31. 점수 계산 원칙

점수는 임의의 숫자를 생성해서는 안 된다.

기본적으로:

```text
원시 측정값
      ↓
기준 집단 분포
      ↓
표준화
      ↓
모델 예측
      ↓
0~100 스케일
```

의 구조를 사용한다.

가능하면 내부적으로 원래의 연속값을 유지하고 UI에서만 0~100으로 변환한다.

---

# 32. 0~100 점수의 의미

반드시 다음 원칙을 지킨다.

**82점 = 사람이 실제로 신뢰할 확률 82%가 아니다.**

**82점 = 절대적인 신뢰성 82/100도 아니다.**

예:

> 이 점수는 해당 분석 기준에서 상대적인 위치를 표현하기 위한 지표입니다.

---

# 33. 첫인상 모델의 학습 구조

권장 구조:

```text
Face Image
   ↓
468 Landmark / Image Representation
   ↓
Feature Extraction
   ↓
Regression Model
   ↓
First Impression Score
```

초기에는 복잡한 모델을 무조건 사용할 필요가 없다.

가능한 모델:

- 선형 회귀
- 릿지 회귀
- 랜덤 포레스트
- 그래디언트 부스팅
- 신경망 회귀

등을 실험하고 검증 성능을 기준으로 선택한다.

---

# 34. 기하학 모델과 이미지 모델 분리

가능하면 다음을 별도로 실험한다.

### 모델 A

```text
468 landmark geometry
→ first impression
```

### 모델 B

```text
face image / visual embedding
→ first impression
```

### 모델 C

```text
geometry + visual embedding
→ first impression
```

세 모델을 비교하여 어느 방식이 가장 안정적인지 검증한다.

제품의 핵심 차별점은 **기하학적 측정 가능성**이므로 geometry-only 모델도 독립적으로 유지한다.

---

# 35. 기존 13,322장 데이터 활용

기존 서비스에서 확보한 13,322장 얼굴 데이터가 있다면 이를 무조건 새로운 학습 데이터로 간주하지 않는다.

먼저:

```text
데이터 출처
촬영 조건
라벨의 의미
라벨 생성 방식
성별
연령
중복 이미지
품질
동의 범위
사용 목적
```

을 검토한다.

특히 기존 데이터가 관상/매력/성격 등의 라벨을 포함한다면 새로운 first-impression 모델의 학습에 바로 사용하지 않는다.

**라벨의 의미가 새로운 제품 정의와 일치하는지 검증한 후 사용한다.**

---

# 36. 첫인상 학습 데이터의 핵심

새로운 모델을 제대로 학습시키려면:

```text
얼굴 이미지
+
인간 평가자의 인상 평가
```

가 필요하다.

예:

```text
이미지 A

신뢰감 있는 인상
평가자 1: 7
평가자 2: 6
평가자 3: 8
...
```

여러 평가자의 평균 또는 분포를 목표값으로 사용할 수 있다.

---

# 37. 단일 정답을 만들지 않는다

가능하면:

```text
Mean
Median
Standard deviation
Distribution
```

을 보존한다.

예:

```text
신뢰감 있는 인상

평균 7.2
표준편차 1.1
```

이는 얼굴 인상이 사람마다 다르게 평가될 수 있다는 점을 반영한다.

---

# 38. 모델 검증

모델 학습 후 최소한 다음을 측정한다.

- MAE
- RMSE
- Pearson correlation
- Spearman correlation
- 교차검증 성능
- 테스트 데이터 성능

가능하면 데이터 분할은 사람 단위로 수행한다.

동일 인물의 여러 사진이 train/test에 동시에 들어가는 데이터 누수를 방지한다.

---

# 39. 성별·연령별 검증

전체 평균 성능만 보지 않는다.

가능하면:

```text
전체
남성
여성
연령대별
촬영조건별
```

성능을 별도로 검증한다.

특정 집단에서 성능이 크게 떨어지는 경우 이를 숨기지 않고 확인한다.

---

# 40. 두 얼굴 "궁합" 모델에 대한 원칙

초기 버전에서는 **실제 연애 궁합을 예측하는 모델을 만들지 않는다.**

대신:

```text
Face A
Face B
    ↓
기하학적 관계
+
첫인상 프로필 관계
    ↓
닮은 정도
조화도
보완도
```

를 계산한다.

이것은 "연애 성공 확률"을 예측하는 것이 아니다.

---

# 41. 닮은 정도 계산

기본 개념:

```text
distance(A, B)
```

를 계산하고 이를 유사도로 변환한다.

예:

```text
similarity = f(distance)
```

구체적인 함수는 데이터 검증을 통해 결정한다.

가능한 방법:

- Procrustes distance
- Euclidean distance
- Mahalanobis distance
- PCA 공간 거리
- 학습된 얼굴 기하학 임베딩 거리

등을 비교한다.

---

# 42. 조화도 계산

조화도는 단순한 similarity의 반대가 아니다.

초기 구현에서는 여러 얼굴 영역의 관계를 이용한다.

예:

```text
형태 비율 관계
+
윤곽 관계
+
대칭성 관계
+
첫인상 프로필의 전체적인 관계
```

을 결합한다.

단, 조화도라는 개념에 대한 객관적 기준 데이터가 충분하지 않다면 **초기에는 연구 가설 기반 지표로 표시**하고, 실제 사용자 평가 데이터를 통해 검증한다.

---

# 43. 보완도 계산

첫인상 프로필을 벡터로 정의한다.

예:

```text
A = [
  warmth,
  approachability,
  dominance,
  energy,
  seriousness
]

B = [
  warmth,
  approachability,
  dominance,
  energy,
  seriousness
]
```

두 벡터의 차이를 계산한다.

단순히 차이가 클수록 보완도가 높다고 정의하지 않는다.

예를 들어 모든 특성이 반대라고 해서 무조건 좋은 보완은 아니다.

따라서 초기에는:

```text
프로필 차이
+
각 특성의 방향
+
경험적 사용자 평가
```

를 기반으로 점진적으로 모델을 개선한다.

---

# 44. "보완도"에 대한 중요한 제품 원칙

보완도는 다음을 의미하지 않는다.

```text
성격 궁합
연애 궁합
결혼 궁합
실제 인간관계 성공률
```

보완도는 오직:

> **두 얼굴에서 형성되는 첫인상 프로필이 서로 다른 방향의 특성을 얼마나 나타내는가**

를 의미한다.

---

# 45. 향후 사용자 데이터 기반 개인 선호 모델

향후 확장 기능이다.

사용자가 여러 얼굴을 보고:

```text
좋아요
별로예요
선호
비선호
```

등의 선택을 하면 해당 사용자의 선호 패턴을 학습한다.

구조:

```text
사용자 얼굴
+
사용자가 선택한 얼굴들
+
각 얼굴의 기하학
+
각 얼굴의 첫인상
```

을 이용한다.

---

# 46. 개인 선호 분석

예:

```text
당신의 얼굴 선호 경향

얼굴 형태
자신과 유사한 형태를 선호하는 경향

눈 영역
눈 영역의 개방도가 높은 얼굴을 선호

첫인상
따뜻하고 친근한 인상을 선호하는 경향
```

이 기능은 **사용자가 실제로 선택한 데이터에 기반한 개인화**를 목표로 한다.

---

# 47. 선호 모델과 성별 연구의 관계

외부 연구에서 성별에 따라 얼굴 유사성에 대한 선호 방향이 다르게 나타나는 연구 결과가 존재하더라도 이를 앱에서 고정 규칙으로 사용하지 않는다.

예:

```text
여성 = 닮은 남성 선호
남성 = 다른 여성 선호
```

같은 하드코딩 규칙을 만들지 않는다.

이유:

- 연구 결과는 특정 표본에 기반한다.
- 문화권이 다를 수 있다.
- 플랫폼이 다르다.
- 연령대가 다르다.
- 개인차가 크다.
- 연구 결과를 보편 법칙으로 확대할 수 없다.

따라서 외부 연구는 **초기 가설 또는 분석 참고자료**로만 사용한다.

실제 서비스에서는 사용자 선택 데이터를 통해 개인별 선호를 모델링한다.

---

# 48. 중요한 UX 원칙

서비스는 사용자에게 "판결"을 내리는 느낌을 주면 안 된다.

### 나쁜 UX

> 당신은 신뢰도가 낮습니다.

### 좋은 UX

> 이 사진은 신뢰감 있는 첫인상을 상대적으로 강하게 만드는 편입니다.

---

### 나쁜 UX

> 두 사람은 궁합이 87점입니다.

### 좋은 UX

> 두 분은 얼굴 형태가 상당히 비슷하며, 첫인상에서는 서로 다른 특성이 나타나 보완적인 프로필을 형성합니다.

---

# 49. 결과 설명의 기본 템플릿

모든 설명은 가능하면 다음 구조를 따른다.

```text
관찰
↓
측정
↓
통계적 관계
↓
해석
```

예:

```text
관찰:
두 얼굴의 눈 영역 구조가 유사합니다.

측정:
눈 영역 기하학 유사도 81

해석:
두 사람은 눈 주변의 얼굴 구조가 비교적 비슷한 편입니다.
```

---

# 50. 금지되는 설명 방식

다음과 같은 논리를 생성하지 않는다.

```text
눈이 작기 때문에 내성적이다.
```

```text
턱이 강하기 때문에 리더십이 강하다.
```

```text
코가 높기 때문에 재물운이 좋다.
```

```text
눈매가 날카로워서 공격적이다.
```

이런 문장은 실제 사람의 심리·성격을 얼굴 형태로 직접 추론하는 관상적 논리로 회귀할 위험이 있다.

---

# 51. 첫인상 설명 방식

대신:

```text
이 얼굴의 특정 시각적 특징이
평가자들에게 특정 인상과 연결되는 경향을
모델이 학습했습니다.
```

와 같이 **지각(perception)**의 관점으로 설명한다.

---

# 52. 서비스 전체 정보 구조

권장 메뉴 구조:

```text
홈
│
├── 내 얼굴 분석
│   ├── 얼굴 기하학
│   ├── 첫인상
│   └── 상세 분석
│
├── 두 얼굴 분석
│   ├── 닮은 정도
│   ├── 조화도
│   ├── 보완도
│   └── 상세 비교
│
└── 내 선호 분석
    ├── 선호 얼굴
    └── 선호 패턴
```

---

# 53. 홈 화면 핵심 메시지

홈 화면은 관상 앱처럼 보이면 안 된다.

권장 방향:

> **내 얼굴은 어떤 인상을 만들까?**

그리고:

> **두 얼굴은 어디가 닮았고, 어디에서 서로 다른가?**

---

# 54. 결과 페이지의 핵심 경험

사용자가 사진을 업로드하면 다음 순서로 결과를 보여준다.

### 1단계

```text
얼굴 측정 중
```

### 2단계

```text
얼굴 기하학 분석
```

### 3단계

```text
첫인상 분석
```

### 4단계

```text
당신의 첫인상 프로필
```

### 5단계

```text
왜 이런 결과가 나왔을까요?
```

이 흐름을 통해 사용자가 단순한 "AI 점수 생성"이라고 느끼지 않도록 한다.

---

# 55. 재미 요소

서비스의 재미는 "점수가 높으면 좋다"에만 의존하지 않는다.

다음 경험을 제공한다.

## 얼굴 지도

내 얼굴의 특징이 어디에 있는지 시각화한다.

## 첫인상 카드

```text
당신은 이런 인상을 줍니다.

따뜻함 ★★★★★
친근함 ★★★★★
주도성 ★★☆☆☆
활력 ★★★★☆
```

## 두 얼굴 비교

```text
어디가 닮았을까?
어디가 다를까?
왜 조화롭게 보일까?
```

## 사진 비교

같은 사람의 사진 여러 장을 비교한다.

```text
사진 A
친근함 81

사진 B
친근함 92

사진 C
친근함 73
```

이를 통해:

> **사진에 따라 내가 전달하는 첫인상이 어떻게 달라지는지**

를 보여준다.

---

# 56. 같은 사람의 사진 비교 기능

매우 중요한 기능으로 취급한다.

입력:

```text
같은 사용자의 사진 A
같은 사용자의 사진 B
```

출력:

```text
첫인상 변화

친근함       +12
활력         +8
진지함       -7
주도성       -3
```

그리고:

> 사진 B는 사진 A보다 친근하고 활기찬 첫인상을 만드는 경향이 있습니다.

라고 설명한다.

이 기능은 실제 사용 가치가 높은 핵심 기능으로 고려한다.

---

# 57. 프로필 사진 분석으로의 확장

향후 다음 용도로 확장 가능하다.

- 프로필 사진
- 자기소개 사진
- SNS 사진
- 업무용 프로필 사진
- 데이트 프로필 사진

단, 특정 직업 적합성이나 실제 능력 등을 판단하지 않는다.

예:

> "이 사진은 업무용 프로필에서 보다 진지한 첫인상을 줄 가능성이 있습니다."

는 가능하지만,

> "이 사진을 사용하면 면접 합격률이 30% 증가합니다."

는 금지한다.

---

# 58. 모델 버전 관리

모든 분석 결과는 반드시 모델 버전을 기록한다.

예:

```text
geometry_model_version: 1.0.0
impression_model_version: 1.0.0
pair_model_version: 1.0.0
```

모델 변경 시 과거 결과와 현재 결과가 달라질 수 있음을 고려한다.

---

# 59. 재현성

동일한 입력 이미지와 동일한 모델 버전에서는 가능한 한 동일한 결과가 나와야 한다.

```text
image
+
model_version
+
feature_version
=
deterministic result
```

난수 기반 모델을 사용하는 경우 seed 및 inference 설정을 관리한다.

---

# 60. 이미지 품질 검사

분석 전에 다음을 검사한다.

- 얼굴이 존재하는가
- 얼굴이 하나인가
- 얼굴 크기가 충분한가
- 얼굴이 너무 기울어졌는가
- 눈이 가려졌는가
- 마스크 등으로 주요 영역이 가려졌는가
- 이미지 해상도가 충분한가
- 조명이 지나치게 어두운가
- 얼굴이 지나치게 흐린가

문제가 있는 경우 억지로 점수를 생성하지 않는다.

예:

> 얼굴이 충분히 선명하게 보이지 않아 정확한 분석이 어렵습니다. 정면에 가까운 사진을 사용해주세요.

---

# 61. 다중 얼굴 사진

여러 명이 포함된 사진은 기본적으로 분석 대상자를 선택하도록 한다.

가능하면:

```text
얼굴 1
얼굴 2
얼굴 3
```

을 감지하고 사용자가 분석 대상을 선택한다.

두 사람 분석에서는 반드시 두 얼굴이 명확히 구분되어야 한다.

---

# 62. 개인정보 및 이미지 처리

얼굴 이미지는 민감할 수 있으므로 데이터 저장 정책을 명확하게 분리한다.

가능하면:

```text
원본 이미지
```

와

```text
분석용 landmark / feature
```

를 별도로 관리한다.

원본 이미지가 반드시 서버에 장기 저장되어야 하는지 먼저 검토한다.

가능하다면 분석 후 원본을 즉시 삭제하는 옵션도 고려한다.

---

# 63. 사용자에게 고지할 핵심 문구

분석 결과 주변에 다음 취지의 안내를 제공한다.

> **이 분석은 얼굴의 시각적 특징과 해당 특징이 만들어내는 첫인상을 분석합니다. 실제 성격, 능력, 도덕성 또는 미래의 행동을 판단하는 결과가 아닙니다.**

두 얼굴 분석:

> **두 얼굴 분석은 얼굴 형태와 첫인상 프로필의 관계를 분석한 결과이며 실제 연애·결혼·인간관계의 성공 여부를 예측하는 기능이 아닙니다.**

---

# 64. 개발자가 절대로 추가하지 말아야 할 기능

다음 요구사항을 임의로 추가하지 않는다.

```text
관상
사주
운세
재물운
연애운
결혼운
범죄 가능성
성격 진단
지능 추정
정신 상태 추정
실제 건강 상태 추정
실제 신뢰성 판정
실제 도덕성 판정
연애 성공 확률
결혼 성공 확률
```

제품 방향이 흔들리는 경우 이 문서를 우선한다.

---

# 65. 용어 사전

서비스에서 다음 한국어 용어를 사용한다.

| 내부 개념             | 사용자 표시      |
| --------------------- | ---------------- |
| Facial Geometry       | 얼굴 기하학      |
| Facial Landmark       | 얼굴 랜드마크    |
| First Impression      | 첫인상           |
| Trustworthiness       | 신뢰감 있는 인상 |
| Approachability       | 친근한 인상      |
| Warmth                | 따뜻한 인상      |
| Dominance             | 주도적인 인상    |
| Energy                | 활기찬 인상      |
| Attractiveness        | 매력적인 인상    |
| Similarity            | 닮은 정도        |
| Impression Similarity | 첫인상 유사도    |
| Harmony               | 조화도           |
| Complementarity       | 보완도           |
| Facial Profile        | 얼굴 프로필      |
| Preference            | 선호             |
| Facial Feature        | 얼굴 특징        |
| Landmark Geometry     | 랜드마크 기하학  |

사용자 대상 UI에서 영어 용어를 그대로 사용하지 않는다.

---

# 66. 제품의 핵심 철학

이 서비스는:

> **"당신이 어떤 사람인지 얼굴로 알아내는 서비스"**

가 아니다.

다음이다.

> **"당신의 얼굴이 가진 시각적 구조를 측정하고, 그 얼굴이 만들어낼 가능성이 있는 첫인상을 분석하는 서비스"**

두 사람 분석은:

> **"두 사람이 실제로 잘 맞는지를 예언하는 서비스"**

가 아니다.

다음이다.

> **"두 얼굴이 어디에서 닮았고, 어떤 첫인상을 만들며, 두 프로필이 어떤 방식으로 함께 나타나는지를 분석하는 서비스"**

---

# 67. 제품의 세 가지 핵심 질문

서비스 전체는 사용자의 다음 세 가지 질문에 답해야 한다.

## 질문 1

> **내 얼굴은 어떻게 생겼는가?**

→ 얼굴 기하학

## 질문 2

> **내 얼굴은 어떤 첫인상을 만드는가?**

→ 첫인상 분석

## 질문 3

> **저 사람과 나는 어떻게 다른가?**

→ 두 얼굴 분석

이 세 질문을 하나의 일관된 분석 체계로 연결한다.

---

# 68. 장기적인 제품 확장 방향

향후 데이터가 충분히 확보되면:

```text
얼굴 기하학
      ↓
첫인상
      ↓
사용자 선택
      ↓
개인 선호
      ↓
두 얼굴 관계
      ↓
실제 사용자 피드백
      ↓
경험적 모델 개선
```

의 순환 구조를 만든다.

중요한 점은 **처음부터 연애 궁합을 예측하려 하지 않는 것**이다.

먼저 측정 가능한 얼굴 데이터와 인간의 지각 데이터를 정확하게 모델링한다.

---

# 69. 성공적인 초기 제품의 기준

MVP에서 반드시 달성해야 하는 것은 다음이다.

### 필수

- 얼굴 검출
- 468 랜드마크 추출
- 얼굴 정규화
- 기하학 특징 추출
- 얼굴 기하학 프로필
- 첫인상 프로필
- 두 얼굴 닮은 정도
- 두 얼굴 첫인상 비교
- 조화도
- 보완도
- 결과 설명
- 분석 불확실성 처리
- 모델 버전 관리

### 후순위

- 개인 선호 모델
- 다중 사진 비교
- 프로필 사진 추천
- 사용자 선택 기반 모델 개선

---

# 70. 구현 우선순위

## Phase 1 — 얼굴 측정

```text
MediaPipe
→ 468 landmarks
→ normalization
→ geometry features
```

목표:

> 안정적인 얼굴 기하학 측정 시스템

---

## Phase 2 — 얼굴 프로필

```text
geometry features
→ normalized scores
→ face profile
```

목표:

> 사용자가 이해할 수 있는 얼굴 측정 결과

---

## Phase 3 — 첫인상

```text
face geometry / visual representation
→ impression model
→ first impression profile
```

목표:

> 인간 평가 데이터와 연결된 첫인상 추정 시스템

---

## Phase 4 — 두 얼굴

```text
Face A
+
Face B
↓
similarity
harmony
complementarity
```

목표:

> 두 얼굴을 비교하는 핵심 사용자 경험

---

## Phase 5 — 개인화

```text
user choices
→ preference model
```

목표:

> "내가 어떤 얼굴을 선호하는가"에 대한 개인화 분석

---

# 71. Claude Code 구현 원칙

Claude Code는 기존 관상 기능을 단순히 이름만 변경하는 방식으로 수정하지 않는다.

다음 순서로 작업한다.

```text
1. 현재 코드베이스 분석
2. 기존 관상 관련 기능 분류
3. 유지 가능한 얼굴 검출/랜드마크 코드 식별
4. 관상 추론 로직과 신규 분석 로직 분리
5. 새로운 도메인 모델 정의
6. 얼굴 기하학 파이프라인 구축
7. 첫인상 모델 인터페이스 구축
8. 두 얼굴 비교 엔진 구축
9. 결과 UI 변경
10. 기존 관상/운세 문구 제거
11. 테스트
```

---

# 72. 기존 코드 수정 원칙

기존의 다음 자산은 가능하면 재사용한다.

- 얼굴 검출
- 이미지 전처리
- MediaPipe 연동
- 468 landmark 처리
- 이미지 업로드
- 결과 저장
- 사용자 계정
- 결제 시스템
- 분석 결과 UI의 기반 컴포넌트

반면 다음은 새 제품 정의에 맞게 재설계한다.

- 관상 점수
- 운세
- 성격 추론
- 궁합 계산
- 설명 문구
- 결과 카드
- 분석 결과 명칭

---

# 73. 도메인 모델 분리

코드에서는 최소한 다음 영역을 분리한다.

```text
facial-geometry/
first-impression/
face-comparison/
preference/
analysis-result/
```

각 분석 모델은 독립적으로 버전 관리할 수 있어야 한다.

---

# 74. 핵심 인터페이스 개념

예:

```typescript
interface FacialGeometryAnalyzer {
  analyze(landmarks: FaceLandmark[]): FacialGeometryProfile
}
```

```typescript
interface FirstImpressionAnalyzer {
  analyze(
    geometry: FacialGeometryProfile,
    imageFeatures?: ImageFeatures,
  ): FirstImpressionProfile
}
```

```typescript
interface FaceComparisonAnalyzer {
  compare(faceA: FaceAnalysis, faceB: FaceAnalysis): FaceComparisonResult
}
```

구체적인 기술 스택은 기존 프로젝트 구조를 우선하며, 인터페이스와 책임 분리를 먼저 확립한다.

---

# 75. 테스트 요구사항

## 단위 테스트

- landmark normalization
- scale normalization
- rotation normalization
- Procrustes alignment
- geometric feature calculation
- similarity calculation
- profile scoring
- score normalization

## 통합 테스트

```text
image
→ landmarks
→ geometry
→ impression
→ result
```

전체 파이프라인을 검증한다.

## 회귀 테스트

모델 버전이 변경되어도 기존 결과와의 차이가 의도된 것인지 확인한다.

---

# 76. 품질 기준

다음 조건을 만족하지 않는 분석은 결과를 생성하지 않는다.

- 얼굴 검출 실패
- 랜드마크 품질 부족
- 얼굴이 지나치게 작음
- 심각한 가림
- 심각한 회전
- 이미지 품질 부족

대신 사용자에게 재촬영을 요청한다.

---

# 77. 가장 중요한 제품 원칙 요약

이 프로젝트에서 다음 문장을 절대 잊지 않는다.

> **얼굴에서 사람을 판단하지 않는다.**

대신:

> **얼굴의 형태를 측정한다.**

그리고:

> **그 형태가 만들어낼 가능성이 있는 첫인상을 추정한다.**

두 사람에 대해서는:

> **두 얼굴의 형태와 첫인상 프로필이 어떻게 관계되는지 분석한다.**

따라서 전체 논리는:

```text
얼굴
 ↓
측정
 ↓
기하학
 ↓
첫인상
 ↓
비교
 ↓
닮은 정도 / 조화도 / 보완도
```

이다.

---

# 78. 최종 제품 정의

이 앱은 **관상 앱이 아니다.**

이 앱은:

> **얼굴 기하학을 기반으로 사람의 첫인상을 정량적으로 분석하고, 두 얼굴의 형태와 첫인상 관계를 시각적으로 비교해주는 얼굴 분석 서비스다.**

핵심 기술은:

```text
468-point facial landmark
+
geometric normalization
+
facial feature extraction
+
machine-learning first-impression estimation
+
pairwise facial analysis
+
user preference modeling
```

이다.

핵심 사용자 경험은:

```text
내 얼굴은 어떤 구조인가?
        ↓
나는 어떤 첫인상을 주는가?
        ↓
왜 그런 인상을 주는가?
        ↓
저 사람과 어디가 닮았는가?
        ↓
우리는 어떤 부분에서 조화를 이루는가?
        ↓
우리는 어떤 부분에서 서로 보완적인가?
```

이다.

---

# 79. Claude Code 작업 시 최우선 원칙

Claude Code는 구현 과정에서 기능을 추가할 때마다 다음 질문을 확인해야 한다.

### 질문 A

이 기능이 **얼굴의 측정**인가?

### 질문 B

이 기능이 **사람들이 느끼는 첫인상에 대한 분석**인가?

### 질문 C

이 기능이 **두 얼굴의 관계에 대한 정량적 비교**인가?

### 질문 D

이 기능이 **사용자의 실제 선택 데이터를 이용한 개인화**인가?

A~D에 해당하지 않는 기능이라면 임의로 추가하지 않는다.

특히 다음으로 되돌아가지 않는다.

> 관상 → 성격 → 운세 → 실제 궁합

---

# 80. 최종 목표

사용자가 앱을 사용한 뒤 다음과 같은 느낌을 갖게 하는 것이 목표다.

> "내 얼굴을 그냥 예쁘다/못생겼다로 평가한 게 아니라, 실제 얼굴 구조를 측정해서 내가 어떤 인상을 만드는지 보여주는구나."

그리고 두 사람을 비교했을 때:

> "우리가 몇 점짜리 궁합이라는 게 아니라, 실제 얼굴 형태에서 어디가 비슷하고 어떤 첫인상 차이가 있는지 보여주는구나."

그리고 최종적으로:

> **재미있지만 단순한 운세 앱이 아니고, 실제 측정과 데이터 분석을 기반으로 한 서비스다.**

를 전달하는 것이 이 제품의 핵심이다.

---

# 81. 구현 요구사항 보강 (2026-09-06 확정)

위 §1~§80 의 제품 정의를 현재 코드베이스에 붙이기 위한 확정 사항. §1~§80 과 어긋나는 곳은 이 절이 우선한다.

## 81.1 첫인상 엔진 — 문헌 기반 계산 모델

- **학습하지 않는다.** 자체 평가자 데이터도, 공개 데이터셋(OMI·Chicago Face DB — 비상업 라이선스)도 쓰지 않는다. §33~§39(회귀 학습·검증)와 §36~§37(평가자 데이터)은 **한국인 평가 데이터가 확보된 뒤의 2단계**로 미룬다. 그때 이 문헌 모델을 기준 모델로 두고 학습 모델과 성능을 비교한다.
- 공개 학술연구(Oosterhof & Todorov 2008, Todorov et al. 2013, Sutherland et al. 2013·2018, Vernon et al. 2014, Rhodes 2006)는 **이론 근거와 feature 선정 근거**로만 쓴다.
- **원 논문의 계수를 MediaPipe 468 landmark 에 대응시켜 쓰는 것을 금지한다.** 논문이 보고한 "어떤 특징이 어느 인상과 어느 방향으로 연결되는가"만 가져와 자체 식을 설계한다. "원 모델을 재현했다"고 주장하지 않는다.
- feature 는 세 층으로 나눠 관리하고, 코드에서도 세 층이 파일과 이름으로 드러난다.

| 층 | 뜻 | 코드 위치 (shared/) |
| --- | --- | --- |
| 1. 학술 보고 관계 | 논문이 검증한 "특징 → 인상" 방향. 출처·방향·주/보조 구분을 상수 표로 | `data/constants/impression_evidence.dart` |
| 2. 재구성 feature | 그 특징을 우리 28 계측 + 대칭성으로 다시 정의한 것 | `domain/services/impression_features.dart` |
| 3. 제품 지표 | 축 점수, 백분위, 두 얼굴 지표, 케미 점수 | `domain/services/first_impression.dart` |

- 근거 없는 전통 관상 규칙·임의 인과("턱이 크므로 리더십")는 어느 층에도 넣지 않는다.

## 81.2 축 4개 (v1) 와 근거 방향

§10 의 7축 중 v1 은 문헌 모델이 있는 **4축**만 낸다. 따뜻함(친근함과 같은 축)·활기참·진지함은 얼굴 구조 연구가 없어 제외. 어려 보임은 Sutherland 의 youthful 축이라 후보로 남긴다.

| 축 (표시명) | 근거 | 방향이 보고된 특징 → 재구성 feature (부호) |
| --- | --- | --- |
| 신뢰감 있는 인상 | Oosterhof & Todorov 2008, Todorov 2013 | 입꼬리 올라감 → 입꼬리 각(+). 눈썹 안쪽 올라감 → 눈썹-눈 거리(+). 눈 큼 → 눈 세로비(+). 대칭 → 대칭성(+) |
| 친근한 인상 | Sutherland 2013·2018, Vernon 2014 | 입 모양(웃는 방향) → 입꼬리 각(+). 눈 개방 → 눈 세로비(+). 눈썹 아치 → 눈썹 곡률(+). 둥근 하안 → 턱 각(+), 하단 풍만(+) |
| 주도적인 인상 | Oosterhof & Todorov 2008, Vernon 2014 | 넓은 얼굴 → 얼굴 비율(−), 턱 폭(+), 광대 폭(+). 낮은 눈썹 → 눈썹-눈 거리(−). 굵은 눈썹 → 눈썹 두께(+). 각진 턱 → 턱 각(−) |
| 매력적인 인상 | Rhodes 2006(평균성·대칭), Vernon 2014(youthful-attractiveness) | 대칭성(+). 평균성 → 28 계측 \|z\| 합(−). 눈 큼 → 눈 세로비(+) |

- **점수 식**: 축 = Σ(부호 × 층위 가중치 × feature z-score). 층위 가중치는 논문에서 주 특징 1.0, 보조 0.5. z-score 는 AAF 11,800 실측 기준.
- **백분위**: 같은 식을 AAF 11,800 얼굴에 돌려 얻은 분포로 백분위 → 0~100. 축의 정의는 문헌, 백분위는 실측. §8 원칙(임의 백분위 금지)을 지킨다.
- **매력 축은 본인 화면에만.** 두 얼굴·케미 어디에도 쓰지 않는다.
- **고정 문구** (모든 첫인상 결과 옆): "공개 학술연구를 기반으로 계산한 추정치이며, 한국인만을 대상으로 학습·검증된 모델이 아닙니다." 정확도·일치율·± 는 검증 전이라 표기하지 않는다. **분석 확신도**(§14)는 사진 상태(정면 각도·얼굴 크기·대칭) 3단으로만 표기하고, 사람의 특성이 아님을 문구로 밝힌다 (2026-09-06).
- **새 계측**: 좌우 대칭 6개(눈·눈썹·코·입·윤곽 + 전체). AAF 11,800장에 같은 식을 돌려 성별 mean/sd/분위표를 만들었으므로(`symmetry_reference.dart`) 화면에도 "상위 N%"(대칭이 높을수록 상위)를 붙이고, 축 점수 안에는 전체 대칭 z 가 신뢰(보조)·매력(주) feature 로 들어간다. 그 외 기하학 파이프라인은 현재 28 계측(`FaceMetrics.computeAll`)을 그대로 쓴다. Procrustes 정렬은 아직 없다(§5·§24·§55 는 랜드마크 저장 여부 결정 대기).

## 81.3 두 얼굴 — 제품 지표 정의

| 지표 | 정의 | 성격 |
| --- | --- | --- |
| 닮은 정도 | 정규화한 계측 벡터의 거리 → 0~100. 영역별(눈·코·입·턱선·윤곽) 부분 점수도 같은 식 | 측정 |
| 첫인상 유사도 | 두 프로필(4축)의 거리 → 0~100 | 측정 |
| 조화도 | 축마다 두 사람 중 높은 쪽을 취해 평균. "둘이 함께 있으면 인상 축이 다 채워지는가" | 제품 정의, 문헌 검증 없음 → "가설 지표" 표기 |
| 보완도 | 축마다 \|A−B\| 의 평균 (매력 축 제외). 두 프로필이 서로 다른 방향을 보이는 정도 | 제품 정의. 성격 보완이 아님(§18·§44) |

닮으면 보완도가 낮고, 조화도는 두 사람의 축 높이에 따라 독립적으로 움직인다. 넷은 서로 다른 값이 나온다. §17 의 "닮은 정도 ≠ 조화도" 를 이 식이 보장한다.

## 81.4 케미 방 — 모드 2종 (확정)

- `teams.mode` 컬럼 추가: `first_impression` / `physiognomy`. 마이그레이션 `0008_team_mode.sql`(idempotent, SQL Editor 수동 적용). 기존 방은 전부 `physiognomy`.
- **Android**: 방 만들 때 둘 중 선택. "첫인상 케미 / 관상 케미".
- **iOS v1**: 선택 없이 `first_impression` 으로만 생성. **`physiognomy` 방은 iOS v1 에서 보이지 않는다** — 목록·상세·참여·매트릭스 전부. 따라서 iOS 빌드에는 관상 쌍 엔진도, "관상" 문자열도 들어가지 않는다. Android 가 만든 관상 방의 초대 링크를 iOS 에서 열면 "이 방은 iOS 에서 볼 수 없습니다" 안내만.
- **첫인상 방 매트릭스 값 = 케미 점수 = 조화도 + 보완도 + 닮은 정도 (세 성분의 합, 0~300).** 셀을 누르면 세 성분이 모두 보인다. 베스트 쌍은 이 값의 최댓값. "궁합" 이라는 단어는 쓰지 않는다.
- 두 모드의 쌍 엔진은 모두 `shared/` 에 두어 웹 참가(JoinWizard)·Android·iOS 가 같은 코드를 쓴다. iOS 는 `first_impression` 채점기만 쓴다.
- 데모 방(`web/db/tests/demo_teams.sql`)은 전부 `first_impression` 으로 다시 만든다.

## 81.5 이름 (확정)

| 자리 | Android | iOS v1 |
| --- | --- | --- |
| 탭 | 관상 · 궁합 · 케미 · 채팅 · 설정 (그대로) | **첫인상 · 비교 · 케미 · 채팅 · 설정** |
| 앱바 제목 | 그대로 | 첫인상 분석 · 두 얼굴 비교 · 케미 |
| 방 모드 표시 | 첫인상 케미 / 관상 케미 | (첫인상 방만 있으므로 표시 없음) |

## 81.6 빌드 구분 (2026-09-06 B안으로 변경)

- 에디션은 **플랫폼 런타임 판정** (`lib/core/edition.dart`): iOS = `measure`, Android = `full`. `flutter run --release` / `flutter build ipa` 에 플래그가 필요 없다. `--dart-define=FACELY_EDITION=measure|full` 은 테스트·디버깅용 우선값.
- 따라서 관상 서술·관상 쌍 엔진·physiognomy 방 코드는 iOS 바이너리 안에 **남는다**. 화면에서 그 경로로 갈 수 없을 뿐이다. 이 값을 바꾸는 원격 설정·런타임 플래그는 없다 — 플랫폼과 컴파일 플래그뿐 (가이드라인 2.3.1 의 "숨겨진 스위치" 에 해당하지 않음). measure 에디션은 `app_config.ios_narrative_version` 을 읽지 않는다.
- A안(컴파일 상수 + tree-shake, `strings` 검사 0)은 형이 "플래그 없이 `flutter run --release` 만 쓰겠다"고 해서 폐기. 다시 A로 가려면 `edition.dart` 의 `final` 을 `const` 로 되돌리고 EditionCopy getter 를 const 로 바꾸면 된다.

## 81.7 이 절이 §1~§80 을 덮어쓰는 곳

| §1~§80 | 보강 |
| --- | --- |
| §10·§12·§29 7축 | v1 4축 (81.2) |
| §14 확신도·± | 확신도는 사진 상태 3단(81.10, 2026-09-06). ± 와 정확도는 미표기 |
| §17·§42 조화도 "데이터로 검증" | 제품 정의 식 + "가설 지표" 표기 (81.3) |
| §33~§39 학습·검증 | 한국인 평가 데이터 확보 후 2단계 |
| §35 13,322장 | 인상 라벨이 없어 첫인상 학습에 쓰지 않음. AAF 는 z-score·백분위 기준으로만 |
| §45~§47 개인 선호(좋아요/별로예요) | v1·v2 모두 밖. 남의 얼굴에 호불호를 누르는 구조라 외모 평가 논란(2026-08 Korea Times)과 닿아 있음 |
| §52 메뉴 구조 | 5탭 유지, 이름은 81.5 |
| §64 관상 금지 | iOS 빌드 기준. Android 는 관상 유지(§0 전략) |

## 81.8 그 밖에 그대로 반영하는 것

- 얼굴 데이터 처리방침 제2조(`web/public/privacy.md`) 는 이미 이 정의와 맞다. "프로파일링 미사용" 조항과 첫인상 지표의 관계는 "얼굴 기하학의 상대 위치"이지 개인 식별·추적이 아니므로 충돌 없음. 다만 첫인상 지표를 서버에 저장한다면 §2.1 수집 항목에 "첫인상 지표(계측값에서 계산한 파생값)" 한 줄을 추가한다.
- Android 사용자가 코인으로 산 관상 궁합 기록은 iOS 로그인 시 보이지 않는다(physiognomy 경로 부재).
- 카메라·앨범의 실시간 계측 오버레이(정면 28·측면 8·각도 상태 pill)는 그대로 iOS v1 의 첫 화면 재료이자 스토어 스크린샷 1장이다.

## 81.9 전통 관상·궁합 해석은 지우지 않는다 (2026-09-06)

- 관상 서술 엔진(`life_question_narrative.dart`, `report_assembler.dart`, `physiognomy_scoring.dart`, 14-node tree, archetype), 궁합 5-frame 엔진(`shared/domain/services/compat/`), 관상 narrative 데이터·테스트는 **삭제·축소하지 않는다.** Android 가 계속 쓰고, iOS 는 추후 별도 대책과 함께 다시 붙인다.
- iOS v1 에서의 부재는 **화면 경로를 쓰지 않는 것(81.6, 플랫폼 판정)**이지 코드베이스에서 없어지는 것이 아니다. §71·§72 의 "재설계" 항목은 첫인상·두 얼굴 경로를 **새로 추가**하는 것이고 관상 경로를 대체하는 것이 아니다.
- 관상·궁합 경로의 iOS 대응 대책은 추후 이 문서에 별도 절로 추가한다.
- **카드 종류 분기 (2026-09-06).** 카드(얼굴 기록 한 장, `metrics` 행)에 `kind` = measure | physiognomy 를 만든 쪽이 박는다. 앱 리포트·비교 상세·비교 목록·웹 `/r/{id}` 가 전부 이 값으로 갈린다. 두 카드 중 하나라도 measure 면 비교(첫인상), 둘 다 physiognomy 면 궁합. iOS 는 종류와 무관하게 관상을 그리지 않는다. measure 카드가 관상 서술을 받거나 physiognomy 카드가 첫인상 리포트를 받는 경로는 없다.

## 81.10 진행 상황 (2026-09-06)

**끝난 것 — 전부 main 에 푸시됨.**

| 단계 | 내용 | 커밋 |
| --- | --- | --- |
| 엔진 | 첫인상 세 층(문헌 관계 → 재구성 feature → 4축 백분위) · 두 얼굴 4지표 · 케미 합 · AAF 분위표(축 4 + 계측 26) | `4ba93a0c` |
| DB | `teams.mode` 0008 (적용 완료) · payload·mode 일치 check | `af68f77d` `14d0f44a` |
| 케미 엔진 | `TeamScoring` 관상/첫인상 분기 · 사분위 등급 · 웹 runTeam mode | `e6d0494b` |
| iOS 문구 | 탭 첫인상·비교·케미 · 앱바 · 빈 화면 · pill · 온보딩 4장 문구 · 관상·궁합 단어 0 | `7412abe4` `3f05acfe` |
| 리포트 | MeasureReportBody(백분위·희귀도) + MeasureShareCard | `1c402fc6` |
| 비교 | MeasurePairBody · 목록 카드 요약 · 해제 기록 · (i) 안내 · 케미 쌍 시트 세 성분 | `98bdfcb4` |
| 메타 | 리스트 배지·케미 참가자 메타 = 첫인상 최고 축 · iOS define 가드 · 빌드 명령 문서 | `2330e662` |
| 온보딩 이미지 | `onboarding{n}-ios/android.png` 8장, 선택 규칙 한 줄 | `f4272e35` |
| 대칭 (§6.1) | 좌우 대칭 6 계측 · AAF reference · 리포트 섹션 · 신뢰/매력 축 feature · 분위표 재생성 | `57108c6a` |
| 프로필·문장 (§7·§13·§25) | 영역별 평균 근접도 프로필(AAF 분위) · 근거 문장형 · 닮은/다른 부분 문구(무작위 쌍 사분위) | `ed3d6cc0` |
| 모델 버전 (§58·§59) | geometry/impression/pair 버전 상수 · 리포트·웹 body·첫인상 payload 기록 · 리포트 카드 · 고정값 회귀 테스트 | `827f4c6f` |
| 사진 품질·여러 얼굴 (§60·§61) | 얼굴 크기·밝기·정면 각도 검사(흐림 제외) · 여러 얼굴이면 번호 상자로 선택 · 고른 얼굴만 mesh·성별 추정 | `089a397d` |
| 5단계 공개·사진 비교 (§54·§56) | 분석 중 단계 문구 3개 → 리포트 직행 · 리포트 = 첫인상 프로필 → 왜 이런 결과 · 비교 탭의 사진별 첫인상(2~4장) | `e8d69715` |
| 카드 종류 분기 (§81.9) | body.kind · 앱 리포트/비교/목록 분기 · 웹 `/r/{id}` measure SSR(runMeasure/runMeasurePair) · seed kind | `ec82e0e4` |
| 비교 상세 보강 (§24·§25·§30) | 무작위 쌍 대비 상위 N% · 점별 차이 지도 · 축별 차이 문장 · 조화도 담당 · 같이/반대로 튀는 계측 · 대칭·얼굴형 | `be658671` |
| 첫인상 카드·확신도 (§55·§14·§29) | 공유 카드 = 매력 제외 3축 5칸 막대 · 분석 확신도 3단(사진 상태: yaw·얼굴 폭·대칭) 리포트·카드 표기 | `5209771b` |
| 얼굴 지도 보강 (§55) | 성별 평균 얼굴(AAF 정규화 평균) 회색 밑그림 + 특이점 3개 계측 측정선을 두 얼굴에 그림 | `d6df0661` |
| 닮은 정도 Procrustes 전환 · 데모 seed 스키마 2 | AAF 11,800장 좌표 추출(f32) · 영역별 거리 중앙값·사분위·케미 등급 경계 재보정(170.3/152.4/134.4) · seed 인물 좌표 | `941baf9a` |
| 좌표 저장·정규화 (§5·§24·§26·§27·§55·§75) | 스키마 2: body 에 landmarks 468×[x,y](등방 원본, 4자리)·lateralLandmarks · 정규화+Procrustes · 얼굴 지도 · 두 얼굴 겹치기 · 처리방침 제2조 개정 · 0009 RLS | `4417aac3` |

빌드: `flutter run --release` / `flutter build ipa` (플래그 없음, 플랫폼 자동). 테스트는 두 에디션 모두 288 green.

**좌표 저장 결정 (2026-09-06, 형 확정).** 사용자가 없는 초기에 breaking change 로 간다 — 스키마 2, 좌표 없는 카드는 폐기.
정규화 좌표는 저장하지 않고 원본 등방 좌표만 저장해 로드 때 계산한다(§59). 처리방침 제2조·영문 요약·수집 항목 표를 고쳤다.

**스키마 2 적용 절차 (형이 직접).**
1. Supabase SQL Editor 에서 `web/db/migrations/0009_metrics_landmarks.sql` 실행 (insert 정책의 landmarks 금지 해제).
2. 스키마 1 행 정리: `delete from public.metrics where (body::jsonb ->> 'schemaVersion')::int < 2;` — 형 폰의 카드 포함 전부 사라진다. 케미 방 snapshot 도 스키마 1 이면 방이 안 열린다.
3. 데모 seed 는 `regen_first_impression_seed.mjs --schema2` 로 재생성됨(인물마다 AAF 실측 얼굴 좌표, 계측·대칭 재계산, 인물 8명의 내 얼굴 카드 행까지). SQL Editor 에서 `demo_teams.sql` 실행 — teams 전체가 지워지고 다시 만들어진다. 데모 계정(홍청)과 실계정 참가자의 내 카드는 앱에서 다시 찍는다. 쌍 상세는 snapshot body + 서버 카드 id 로 연다 (2026-09-06).
4. web 빌드·배포 (`facely.kr/privacy.md` 제2조 확인) · Android 배포 시 `app_config.android_min_build` 상향.

**남은 사양 항목.**
- [x] §55 첫인상 카드 — 5칸 막대, 매력 제외 (2026-09-06)
- [x] §29 confidence — 사진 상태 기반 3단 (2026-09-06)
- [ ] §6 얼굴 면적·윤곽 곡률·양 눈 상대 크기·코 중심축 계측, §60 흐림 판정 — 필요하면 추가

**남은 것 — 제출 전.**

- [x] 실기기(iPhone, 카카오톡 미설치): 인앱 로그인 시트, 오버레이 라벨 열, 첫인상 리포트·비교·케미 화면 한 바퀴 (2026-09-06 완료)
- [x] 데모 방 seed — first_impression 방 · 스키마 2 · 인물 카드 행까지 재생성, 운영 적용 (2026-09-06)
- [x] 웹 공유 페이지: measure 카드는 첫인상 SSR, measure 가 낀 쌍은 비교 SSR (2026-09-06). 웹 재배포 필요
- [ ] 스토어 메타데이터: 부제·설명·키워드·스크린샷 5장(1장 = 계측 오버레이) · 카테고리 라이프스타일 · 리뷰 노트 · Resolution Center 답변(4.3(b) 컨셉 변경 · Guideline 4 수정 · 2.1 처리방침 제2조 인용)
- [x] web 빌드·배포 → `facely.kr/privacy.md` 제2조 확인 (2026-09-06, 버전 65cbbdbb — 얼굴 특징점 좌표·첫인상 지표·영문 요약 반영)
- [ ] App Store Connect 개인정보 라벨 — 제2조와 맞춘다 (얼굴 특징점 좌표·계측값·썸네일·추정 범주)
- [ ] Android 배포 시 `app_config.android_min_build` 상향 (구버전이 첫인상 방에 관상 payload 쓰는 것 차단 — 서버 check 가 이미 막지만 화면 혼선 방지)

**iOS 에서도 아직 남아 있는 관상 흔적 (허용 범위).**
- 앱 이름 "관상은 과학이다" — 전략상 유지.
- emotion-* 마스코트(점술가 캐릭터) — 앱의 마스코트다. **바꾸지 않는다** (2026-09-06 확정). 빈 화면·앱 정보에 그대로 쓴다.
- 카카오 초대 문구의 앱 이름.
