# PRD — 관상은 과학이다

**문서 버전**: 1.0 (2026-07-09)
**작성 근거**: docs 3종(ARCHITECTURE·HOW-IT-WORKS·DESIGN) + PIVOT.md 확정 스펙(git 이력) + KAKAO.md + 실코드·커밋 대조
**상태 스냅샷**: 케미 배틀 rev2(방 유형·매칭 성사·인앱 채팅) 코드 완료 · `flutter test` 161 green · Supabase rev2 재적용·실기기 E2E·웹 스모크·운영 장치·스토어 재제출 미완
**남은 작업 실행 목록**: 본 문서 §6.2 (flutter/README.md 는 빌드 안내로 축소됨)

---

## 1. 제품 개요

### 1.1 한 줄 정의

> **아는 사람들끼리 얼굴로 노는 관계 콘텐츠 앱.** 온디바이스 안면 계측(MediaPipe 468 landmarks)으로
> 관상을 수치화하고, 그 측정값으로 1인 관상 풀이 · 2인 궁합 · **다인 케미(팀 N×N 케미 결과표)** 를 제공한다.

### 1.2 제품 분류 체계

| 제품          | 인원   | 역할                                                                                                                 |
| ------------- | ------ | ---------------------------------------------------------------------------------------------------------------------- |
| **관상**      | 1인    | 콘텐츠 층 — 26+8 metric → 14-node → 10 attribute → archetype → 8 인생 질문 본문                                        |
| **궁합**      | 2인    | 五行·十二宮·五官·三停·陰陽 5-frame 별도 엔진, 4단 라벨                                                                 |
| **케미 배틀** | 6~12인 | **1차 기능** — 게임 로비. 방을 만들면 참가자가 셀프 조인하고, 정원(하드 리밋)이 차면 케미 결과가 자동 공개된다. 방 유형 2종: `all`(전체 케미 N×N) / `match`(남녀 반반 이성 케미, 남×여 직사각 매트릭스). 초대가 기능이 아니라 전제인 콘텐츠 |

용어 규칙: 한국어 공식 명칭 = **`케미 배틀`**(2026-07-16 게임 로비 전환·`케미` 단독 표기 폐기), 코드 식별자 = 영문 `team_*`. 결과표 카피는 "생성" 언어만 ("발표/마감" 표기 폐기 2026-07-12).
멤버는 이름 선등록이 아니라 로그인 계정으로 셀프 조인한다 — 방장이 카메라로 멤버를 등록하는 `직접촬영` 액션은 정원 충족 자동 시작 모델로 대체되어 폐기(2026-07-16).

### 1.3 왜 케미가 1차 기능인가 (기각 이력 요약)

| 검토안                          | 기각 사유                                                                    |
| ------------------------------- | ---------------------------------------------------------------------------- |
| 시술상담·안경 피팅 유틸         | 해석 코퍼스 90% 사장, 더 나은 대안 실존                                      |
| 웹 결제 퍼널                    | 무명 사이트 웹 결제는 신뢰 없음 → **결제는 전부 앱스토어 IAP**               |
| 연예인 닮은꼴                   | 부정경쟁방지법 + metrics 자체가 생체인식 특징정보(민감정보)                  |
| 일반인 닮은꼴 SNS / 공개 게시판 | 공유 준거 없음, 콜드 스타트, day-2 루프 부재. 모르는 타인 탐색은 본질이 아님 |

살아남은 구조: **"우리 팀 궁합표"는 혼자 존재할 수 없다** — 초대 자체가 바이럴 루프.
성별 무관 우정·케미 프레임이라 단톡방에 던질 수 있고(데이팅 거부감·4.3(b) dating 회피),
궁합 엔진의 pure 톤이 이미 비연애 페어용으로 구현되어 있다.

---

## 2. 목표와 성공 기준

### 2.1 비즈니스 목표

1. **App Store 4.3(b) 돌파** — 케미라는 운세 템플릿 앱 어디에도 없는 1차 기능 + 온디바이스 468 landmark 측정 기술을 근거로 재제출. Android 선출시로 결제·바이럴 검증 후 iOS 재제출.
2. **설치 1명 → 영업사원 N명 구조** — 현장 경로(상세 페이지 QR 셀프 조인)와 원격 경로(카톡 초대 링크)가 각각 바이럴 루프를 만든다.
3. **코인 경제 단가 보호** — 매트릭스는 "대략 누가 잘 맞나"까지 무료, 숫자·풀이는 유료(1🪙). 1:1 궁합 단가와 충돌 없음.

### 2.2 제품 완성 기준 (출시 게이트)

- [ ] 2기기 시나리오 통과: A폰 배틀 생성 → 카톡 초대 → B폰 조인 → 정원 충족 자동 시작 → `/g/{id}` 웹 쇼케이스
- [ ] 현장 시나리오 통과: 배틀 생성 → 상세 페이지 QR 로 셀프 조인 → 정원 충족 자동 시작 → 매트릭스(🏆 무료·내 행 고정) → 1🪙 unlock → 상세
- [ ] 웹 참여 실기기 통과: 카톡 인앱 → 외부 브라우저 탈출 → 카카오 로그인 → (비밀방) PIN → 카메라 → 조인 완료 → 설치 후 rehydrate 복원
- [ ] 48h 자동 종료 + 24h revealing 안전망 + 30일 purge 서버 cron 동작 (방치 방이 영원히 열려 있지 않음) — 구현·배포됨(`react/workers/cron.ts`), 실동작 확인 남음
- [ ] Android 프로덕션 출시 → iOS 재제출 패키지 제출

---

## 3. 타겟 사용자와 핵심 시나리오

### 3.1 페르소나

| 페르소나            | 상황                                          | 행동                                                                                                        |
| ------------------- | --------------------------------------------- | --------------------------------------------------------------------------------------------------------- |
| **방장(호스트)**    | 회식·MT·동아리 모임에서 분위기를 만드는 사람  | 배틀 방 생성(방 유형·제목 프리셋·인원·연령대·공개/비밀·썸네일 공개 설정) → 상세 페이지에서 QR·카톡·링크로 초대 → 정원 충족 시 자동 결과 공개 |
| **참가자(앱 설치)** | 상세 페이지 QR 을 스캔했거나 링크를 받은 사람 | 로그인 → 내 관상 확인(없으면 촬영) → (비밀방) PIN 입력 → 사진 공개 계약 확인 → 셀프 조인 → 상세 페이지 실시간 슬롯 확인 |
| **미설치 수신자**   | 카톡으로 링크만 받은 사람                     | `/g/{id}` 웹 초대장 → **카카오 로그인 + 웹 캡처로 셀프 조인** → 스토어 → 설치 → 같은 계정 로그인 시 자동 복원 |
| **솔로 사용자**     | 자기 관상이 궁금한 사람                       | 촬영/앨범 → 관상 리포트 → 공유 카드(`facely.kr/r/{id}`)                                                     |

### 3.2 대표 시나리오 — 회식 (현장 + 원격 혼합)

현장/원격은 **구현 경로일 뿐 사용자 선택지가 아니다.** 기본 시나리오가 혼합이다:
방장이 회식 자리에서 8인 방을 만들고, 상세 페이지 QR 코드를 테이블에 띄워 그 자리 인원이
스캔해 셀프 조인하는 동안 못 온 2명은 카톡 초대 링크로 합류한다. 정원이 차는 순간
전원의 화면에서 동시에 3-2-1 카운트다운 → 🏆 베스트 케미 공개. 베스트 쌍 두 사람에게만
매칭 성사 카드가 떠서 서로의 사진이 공개되고, 둘 다 수락하면 인앱 1:1 채팅방이 열린다.

### 3.3 참가 요구사항 (2분기)

초대 링크를 받은 사람은 자신의 상태와 무관하게 **막히지 않아야** 한다:

| 수신자 상태             | 요구사항                                                                                                             | 구현 상태                                                        |
| ----------------------- | ---------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------- |
| 앱 사용자               | 로그인 → 내 관상 있으면 바로 `join_team`, 없으면 촬영 후 조인                                                       | ✅ 구현 (`BattleDetailScreen`)                                     |
| 앱 미사용자              | React 웹(`/g/{id}`)에서 **셀프 조인 완결** — 카카오 로그인 → (비밀방) PIN → 사진 공개 계약 → 정면 캡처 → `join_team`  | ✅ 구현 (`JoinWizard`)                                             |
| 앱 미사용자 → 설치 전환 | **웹 캡처의 관상 데이터를 앱에서 재사용** — 설치 후 다시 찍지 않는다. React 앱도 **카카오 로그인** 지원              | ✅ 구현 (앱 로그인 rehydrate + react 카카오 로그인·capture 귀속) |

---

## 4. 기능 요구사항

### 4.1 케미 배틀 (1차 기능)

#### 배틀 생성

- 홈/케미 탭 [배틀 만들기] → **바텀 슬라이드 풀페이지 스텝 플로우**:
  방 유형(전체 케미/이성 케미) → 제목(카테고리→제목 2단 프리셋 선택, 직접 입력 없음)
  → 참가 인원(6/8/10/12 chip, 기본 8) → 참가 연령대(방장 나이대 포함 인접 2-decade
  RangeSlider — 예: 30대 방장은 20~39세 또는 30~49세) → 공개 설정(공개/비밀 4자리
  PIN + 썸네일 공개 여부) → [배틀 만들기] = 방 생성 + 방장 셀프 조인.
- 연령 하한은 전면 20세 — 10대는 생성 진입 시 사용불가 안내로 차단(서버 CHECK 동반).

#### 정원 정책

**6/8/10/12명, 정원 = 하드 리밋 = 유일한 시작 조건.** 방장 수동 시작·부분 인원 자동 시작은
없다 — 정원이 차면 그 조인 트랜잭션 안에서 즉시 시작(`chemistry_snapshot` 동결 + 상태
`revealing` 전이), 48h 안에 안 차면 cron 이 `expired` 처리한다. "N명 방이면 N명이 온다"는
자연 기대와 일치. match 방은 성별 정원이 각 절반 — 한쪽 성별이 다 차면 `GENDER_FULL`
("남자/여자 자리가 다 찼습니다")로 거부되고, 남녀 모두 차야 정원 충족이다.

#### 상세 페이지 (참가·대기 통합 — 단일 진입점)

- 참가 여부와 무관하게 방의 상세는 이 한 화면 — 미참가자에겐 참가 폼, 참가자에겐
  대기 뷰가 같은 화면 안에서 분기되고, 조인 성공도 화면 전환 없이 참가자 뷰로 바뀐다.
- 헤더: 방 제목 + 연령 pill + "n/N명" 진행.
- 슬롯 리스트: full-width 행(아바타 좌 + 이름·"50대 남성 아시아인" 인구통계·
  "신의형 · 호감형 기질" 관상 유형 meta 우, 내 행은 아바타 좌상단 '나' badge).
  입장이 실시간 반영된다(Supabase Realtime — `teams` UPDATE + `team_members`
  INSERT/DELETE 구독 + 10초(앱)/15초(웹) 백업 폴링 상시 병행, 이탈 반영은 폴링이
  커버). match 방은 남좌·여우 2열 섹션(헤더 + 성별 아이콘). 썸네일 비공개 방은
  **모집 중에 한해** 아바타 대신 성별 기본 아이콘(male/female.png) — 결과 발표
  후에는 참가자 전원 사진 공개가 조인 계약이다 (`thumb_open` 은 모집 중 노출만
  제어).
- **QR 코드**: 같은 자리 참가자가 스캔해 셀프 조인 — 현장 참여의 유일한 경로다. 초대
  3버튼(카톡/링크 공유/복사) 상시 공존 (참가자 뷰).
- 참가자는 [나가기] 가능(방장은 방 삭제만). 정원 충족 시 시작 버튼 없이 자동 전환된다.
- 미참가자 뷰(딥링크 `/g/:id` 진입 포함): 방 정보 미리보기(match 방은 남은 성별 자리
  표기) → 로그인 → 내 관상 보장(없으면 촬영) → (비밀방) PIN 입력 → 사진 공개 계약
  확인 → `join_team` RPC 로 셀프 조인.
- 정체성은 로그인 계정(`users.nickname`) 하나뿐 — 이름 슬롯·빈 슬롯 claim 은 없다.

#### 결과 화면

- 3-2-1 카운트다운 → 🏆 베스트 케미 카드(raw total 최고점 쌍, 점수 무료 공개) → 밴드
  매트릭스(`all` 방 = N×N, `match` 방 = 남×여 직사각 — 이성 쌍만 계산·저장·표시. 셀 =
  4단 밴드 색 점(앱은 BandDot 단일 위젯 — 초록 천생연분 / 파랑 금슬화합 / 주황 상부상조 / 빨강 형극난조, 점 가운데 점수 상시 노출(snapshot 로컬 재계산 — payload 는 여전히 best.score 만), 웹만 이모지·점수 없음),
  **보는 사람 행 최상단 고정**) → 쌍 탭 = 1🪙 unlock(`unlock_compat` RPC).
- 계산은 클라이언트가 한다(`runBattle`, 결정론이라 전원이 같은 결과) — 최초 도달
  클라이언트가 `submit_team_result` 로 결과를 1회 기록(first-writer-wins), 이후
  열람은 그 payload 를 그대로 렌더.

#### 매칭 성사 + 인앱 채팅 (방 유형 무관)

- 베스트 쌍 두 사람에게만 결과 직후 매칭 성사 카드 — 서로의 200×200 사진 공개(성사
  단계는 `thumb_open` 무관 항상 공개, 조인 시 사진 공개 계약이 근거) + "채팅방을
  열까요?" 상호 동의(`respond_match` RPC, 수락/거절 1회 — 재응답 불가).
- 둘 다 수락 → 인앱 1:1 채팅(`team_messages`, Realtime) — 내 배틀 탭에 [채팅] 진입점.
  한쪽 거절 → 즉시 종결("이번에는 채팅방이 열리지 않았습니다"). 무응답 → 30일 purge 와
  함께 자연 소멸. 채팅 수명 = teams 30일 purge cascade.
- 베스트 쌍이 아닌 참가자에겐 이 단계 자체가 없다.

#### 차단 (SNS 안전장치 — 비용은 전부 차단자 부담)

- 차단당한 쪽은 아무 불이익이 없고 차단 사실을 눈치챌 단서도 없다 — 어느 방이든
  참가 가능. 차단자는 상대가 있는 방에 못 들어간다(`BLOCKED_MEMBER`, 사실대로 안내
  — 자기 차단 목록은 본인이 아니 새는 것이 없다).
- 서로가 만든 방만 **상호 비공개** — 목록(`public_teams`)에서 양방향 숨김, 링크
  직접 진입도 `NOT_FOUND`(존재하지 않는 방과 같은 중립 코드).
- 차단하는 순간 두 사람이 같이 있던 모집 중 방에서는 차단자가 자동 퇴장(트리거).
  차단자가 방장인 방만 예외로 상대가 나가진다(방장 자리 보전).
- 제3자 방에서 같이 배틀하게 되면 그 쌍의 발표 점수를 엔진이 **상한 60점**(형극난조
  경계 61.5 아래)으로 눌러 발표한다 — 결과표가 낮은 점수를 그대로 보여주므로
  자기모순(1등인데 채팅 없음)이 없고, 베스트·매칭 카드로 이어지지 않으며, 차단당한
  쪽에는 "궁합이 나쁘다"로만 보인다. 차단 쌍은 시작 시 `chemistry_snapshot.blocked`
  ([[slotA,slotB]])로 동결 — 전 클라이언트 동일 재계산. 시작 후에 생긴 차단은
  `submit_team_result` 가 매칭 카드 생성 직전 한 번 더 걸러낸다.
- 유료 풀이(1🪙)는 실제 관상 분석 그대로 — 상한은 배틀 결과표에만 적용된다(같은 두
  얼굴을 궁합 탭에서 돌리면 어차피 실제 점수가 나오는 상품이므로 풀이까지 조작하지
  않는다).

#### 웹 표면 (`facely.kr/g/{id}` — 한 라우트, 상태 4분기)

- **모집 중 = 초대장 + 웹 참여 위저드(JoinWizard)**: 방 이름·"n/N명"·연령대(match 방은
  남은 성별 자리) 노출 + [카카오로 참여하기] → 카카오 로그인(supabase-js PKCE, 앱과
  같은 `auth.users`) → (비밀방) PIN → 사진 공개 계약 → 정면 캡처(MediaPipe client-only)
  → `runMetrics` → metrics(`user_id`·`is_my_face`) + R2 200px 썸네일 저장 →
  `join_team` RPC 로 셀프 조인. 카톡 인앱 웹뷰는 카메라가 막혀 있어 **외부 브라우저
  자동 탈출** 후 재오픈.
- **결과 공개(payload 있음) = 쇼케이스**: 🏆 베스트 케미 카드 + 밴드 매트릭스(`match`
  방은 남×여 직사각 — `players[].gender` 사용). 사진 없음, 점수는 베스트만. 매칭
  성사·채팅은 앱 전용 — 웹은 "앱에서 확인" 안내.
- **결과 공개(payload 없음, snapshot 있음)**: 브라우저가 `runBattle` 로 즉석 계산해
  렌더하고, 로그인 참가자면 `submit_team_result` 로 정본을 backfill.
- **종료**(`expired`, 또는 `completed` 인데 payload·snapshot 둘 다 없음): 종료 안내.
- 공개 배틀 목록은 이번 범위 밖(앱 전용) — 웹은 링크 조인 + 쇼케이스만.

#### 웹 캡처 데이터 재사용

> 웹에서 이미 얼굴을 찍은 사람에게 앱 설치 후 재촬영을 시키지 않는다 — 설치 퍼널의 마지막 마찰 제거.

- **React 앱 카카오 로그인**: Supabase Auth OAuth (supabase-js PKCE) — 앱과 **같은 프로젝트·같은 계정 체계**. 선행 조건: Supabase Auth Redirect URLs 에 `https://facely.kr/g/*` 등록.
- **capture 귀속 저장**: `runMetrics` 결과(정면 26 metric) + 수동 선택 demographics 를 `metrics` 테이블에 `user_id` 귀속 + `is_my_face=true` 로 upsert, 캡처 프레임은 200px 썸네일로 R2 저장.
- **앱에서 자동 복원**: 설치 후 같은 카카오 계정 로그인 → 로그인 rehydrate 가 그 capture 를 내 관상으로 복원. 참가한 배틀은 `team_members` 가 서버에 이미 갖고 있어 로그인 즉시 "내 배틀" 목록에 그대로 뜬다 — 별도 복원 로직이 필요 없다.
- **측면 metrics 는 fallback**: 웹 캡처는 정면 1장 — 측면 8 metric 없이 저장하고 엔진은 그대로 수용(측면은 옵션 입력). 배틀 조인·궁합 계산은 정면 capture 만으로 성립. 측면 보완 촬영은 앱에서 선택(리포트 정밀도 향상 유도).
- **DeepFace 추정을 웹에도** (보류): 성별/나이 자동 prefill 은 미구현 — 현재는 수동 선택. 필요 시 R2 `temp/` presign + `/analyze` 재사용으로 승격 가능.

### 4.2 관상 (1인)

- 진입: nudge 배너 [내 관상 등록하기] 또는 관상·궁합 탭 [상대방 관상 추가] pill → 카메라(mesh overlay + 자동 카운트다운 2초, 정면 + 측면 yaw 45~80°) 또는 앨범(square-padding 처리) → 성별·연령·인종 확인(DeepFace 자동 prefill) → 리포트.
- 리포트: 10 attribute bar(탭 → top-5 contributor) · 음양 bar · 삼정 radar · 14-node expandable(밴드별 본문) · archetype + 8 인생 질문 본문 + special archetype.
- 본문 톤: **현대 한국어 평문** — 한자 단독 표기 금지, 메타포·자기계발 jargon 금지, 관찰→해석·행동 vignette·이중성 훅·2인칭 경험 예언·한 줄 평 6-레버.
- 공유: R2 thumbnail + Supabase upsert + `facely.kr/r/{uuid}` — 수신 측은 앱 딥링크 또는 웹 SSR 카드(같은 엔진을 JS 컴파일로 line-by-line 동일 렌더).
- 저장 원칙: **capture-only** — raw metric 만 저장, 점수·해석은 load 시 현재 엔진으로 항상 재계산. 엔진이 좋아지면 과거 리포트도 자동으로 좋아진다.

### 4.3 궁합 (2인)

- 두 리포트 짝짓기 → 총점 + 4 sub-score(五形和 0.20 / 宮位調 0.40 / 氣質合 0.25 / 性情諧 0.15) + 4단 라벨.
- intimacy 톤 3분기(pure/flirty/spicy — 성별·연령 조합으로 narrative 만 분기).
- 전체 본문 = 1🪙 unlock. 결제 시점에 두 사람 body 를 `unlocks` 에 동결(self-contained) — 서버 row 가 사라져도 구매한 궁합은 복원된다.

### 4.4 계정·코인·수익화

| 항목      | 스펙                                                                                  |
| --------- | ------------------------------------------------------------------------------------- |
| 인증      | Kakao OAuth · email+OTP. 계정 삭제 = service-role cascade                             |
| 코인      | 원장 테이블(coins) + RPC 단일 트랜잭션. store_transaction_id unique 로 중복 결제 차단 |
| 무료 코인 | AdMob rewarded video 3편 = 1🪙, KST 자정 reset                                         |
| 유료      | 앱스토어 IAP 로 코인 구매 (웹 결제 없음)                                              |
| 과금 지점 | 궁합 전체 본문 1🪙 · 케미 배틀 쌍 상세 1🪙 (`unlock_compat` RPC 그대로)             |
| 무료 유지 | 배틀 방 생성 · 셀프 조인 · 밴드 매트릭스 · 🏆 베스트 케미 1쌍 · 관상 리포트           |

---

## 5. 비기능 요구사항

### 5.1 프라이버시·법적 가드레일

- **얼굴 사진은 온디바이스 처리 원칙.** 서버 업로드는 metrics(숫자) + 썸네일(선택)만. 원본 분석 이미지는 R2 `temp/` 1일 자동 삭제.
- metrics 는 생체인식 특징정보로 취급 — 연예인·실존 인물 비교 기능 영구 금지.
- 타인 등록 = 본인 캡처 동의 전제(등록 진입부 동의 UX).
- 웹 공개 뷰는 이름 + 밴드만 기본, 썸네일은 방장 옵트인.
- 링크 read = UUID 아는 사람(링크 공유 모델) — 유출 시 그룹명·멤버 이름 노출됨을 인지한 설계.

**데이터 수명주기 (worker cron `react/workers/cron.ts`)** — 로그인 rehydrate(관상·궁합 복원)과 한 쌍. 케미 배틀은 서버 우선이라 로그인만 하면 `team_members` 조회로 즉시 뜬다 — 별도 복원 로직이 없다.

- **탈퇴** (`/api/account/delete`): 본인 metrics 전부 hard delete + R2 썸네일 삭제 + 모집 중(open, `closed_at is null`) teams 삭제. metrics FK 는 `on delete cascade` — endpoint 우회 경로(관리 콘솔 등)에서도 고아 row 가 남지 않는 안전망.
- **90일 미활동 anon metrics**: `user_id IS NULL` + `updated_at < 90일` row 를 매일 cron(`cleanupStaleMetrics`)이 R2 썸네일과 함께 삭제. 공유 링크 조회가 `updated_at` 을 touch 하므로 아직 보는 카드는 생존. 로그인 유저 소유 row 는 cron 대상 아님 (내 관상 백업 보호 — 삭제는 탈퇴만).
- **48h 자동 종료**: 모집 48h 초과한 `recruiting` 방을 매시 cron(`expireStaleTeams`)이 인원 무관 `expired` 처리 — 정원 충족은 `join_team` RPC 트랜잭션이 그 자리에서 처리하므로 cron 은 시작을 수행하지 않는다. `completeOrphanReveals` 가 `revealing` 24h 고아 방 completed 안전망을 추가로 담당.
- **완료/종료 후 30일 teams 삭제**(`purgeExpiredTeams`): `result_payload` 의 멤버 실명 보존 기한. 만료 *표시*는 클라이언트가 `closed_at + 30일` 계산, cron 은 실삭제만.
- **`unlocks.partner_body` 는 의도된 보존**: 코인으로 구매한 궁합의 self-contained 스냅샷 — 상대가 탈퇴해도 구매자의 결과는 유지된다 (숫자 metrics 만, 사진 썸네일은 탈퇴 시 R2 에서 삭제되므로 재식별 위험 낮음).

### 5.2 품질 게이트

- `flutter analyze` 기준선 7건(경미) 외 신규 0 · `flutter test` 전부 green (현재 161개).
- 엔진 룰·reference·quantile 변경은 `shared/` 한 곳에서만 + Monte Carlo 재보정(20,000 샘플, seed=42) 동반.
- UI 통일성 절대 1순위 — 디자인 토큰 SSOT(`flutter/docs/DESIGN.md`), 같은 역할 = 같은 토큰, 신규 색상 도입 금지, FontAwesome only.
- Hive·Supabase 스키마는 drop-recreate 자유(출시 전). DDL 은 `0001_baseline.sql` 단일 파일 직접 수정.

### 5.3 아키텍처 제약 (요약)

```
shared/ (face_engine, 순수 Dart SSOT) ── path dep ──▶ flutter/ (앱 셸)
        └── dart compile js -O1 ──▶ react/ (facely.kr Workers — share 카드 SSR + /g 초대장·쇼케이스·웹 티저)
python/ (DeepFace FastAPI — 성별·연령·인종 추정)     Supabase (metrics·coins·unlocks·teams·team_members·team_matches·team_messages)
```

- 궁합 엔진은 Flutter 에만 — react 는 `runBattle`(웹 즉석 계산·backfill)로 같은 shared 엔진을 호출한다. 렌더는 `result_payload` 가 있으면 그대로, 없으면 계산 결과.
- JS export 는 `runEngine` / `runCompat` / `runMetrics` / `runBattle` 4개.
- **실시간 = Supabase Realtime** — 케미 배틀 상세 페이지는 `teams`(UPDATE) + `team_members`(INSERT/DELETE) `postgres_changes` 구독 + 10초(앱)/15초(웹) 백업 폴링을 상시 병행한다(이탈 반영은 DELETE 필터 매칭 한계로 폴링이 커버, 백그라운드 push 알림은 범위 외). 매칭 성사(`team_matches` UPDATE)·인앱 채팅(`team_messages` INSERT)도 같은 Realtime — 쌍 외 수신은 RLS 가 차단.

---

## 6. 현재 상태와 남은 범위

### 6.1 완료 (코드 + 배포)

| Phase | 내용                                                                                  | 상태                         |
| ----- | ------------------------------------------------------------------------------------- | ---------------------------- |
| P1    | 홈 개편(그룹 카드 중심) + 내 관상 셀프 등록 + in-app 스플래시                         | ✅ 기기 검증까지 통과         |
| —     | 케미 배틀 — 게임 로비 전환(teams/team_members RPC 상태 머신·Realtime 상세 페이지·QR 셀프 조인·`result_payload`·cron 4종) + rev2(방 유형 all/match·성별 정원·썸네일 공개·매칭 성사·인앱 채팅·공약 폐기) | 🔶 코드 완료, Supabase rev2 재적용·웹 배포 남음 |
| —     | 카톡 FeedTemplate 초대 · 측면 캡처 yaw 밴드 확대                                      | ✅ 커밋 완료                  |

### 6.2 미완 (우선순위 순)

1. **🔴 출시 차단**: **Supabase rev2 baseline 재적용(drop-recreate) + smoke ALL PASS** · 케미 배틀 실기기 E2E(2기기·현장 QR 조인·match 방 6인 시나리오) · 웹 스모크 검증 · cron 4종(48h expired·24h revealing 안전망·30일 teams purge·90일 anon metrics 정리) 실동작 확인 · **Supabase Redirect URLs 에 `https://facely.kr/g/*` 등록(대시보드 1회)**
2. **🟠 운영 편의**: 공개 배틀 목록 웹 미노출(앱 전용, 의도적 범위 밖)
3. **🟡 운영 + 스토어(P4)**: **이메일 비밀번호 재설정(찾기)** — 앱에 기능 부재. 현재 락아웃 시 운영자가 대시보드에서 수동 리셋뿐(2026-07-12 실제 발생). recovery 메일 발송 + `facely.kr` 재설정 페이지 필요 · 30일 수명주기의 클라이언트 만료 표시(서버 cron 은 구현됨) · 신고·차단·방 제목 필터 · 시즌 템플릿 · 스토어 패키지(Android 선출시 → iOS 4.3(b) 재제출)
4. **⚪ 코드 위생**: analyze 기준선 7건 정리

### 6.3 의도적 범위 제외 (non-goals)

- deferred deep link(설치 직후 자동 입장) — 외부 SDK 필요, "카톡 재탭" 패턴으로 대체. 수요 관측 후 재검토.
- 13명+ 대형 팀 랭킹 뷰 — 수요 관측 후.
- 궁합 엔진의 웹 이식 — 앱 계산·서버 보관·웹 렌더 분업 유지.
- 익명 그룹 소유 — 원격 그룹 방장은 로그인 필수.

---

## 7. 성공 지표 (KPI)

| 구간   | 지표                                                                      | 측정         |
| ------ | ------------------------------------------------------------------------- | ------------ |
| 획득   | `/g/{id}` 웹 방문 → 웹 참여 완료율 · 스토어 클릭 전환율                    | analytics    |
| 활성화 | 신규 설치 → 내 관상 등록 완료율, 초대 링크 → 합류 완료율                  | 앱 이벤트    |
| 바이럴 | 그룹당 평균 멤버 수, 그룹 1개 → 파생 신규 그룹 생성률(쇼케이스 공유 루프) | teams 테이블 |
| 수익   | 매트릭스 열람 → 페어 unlock 전환율, 무료 코인(광고) vs 구매 코인 비율     | coins 원장   |
| 리텐션 | 완료 그룹 보유 사용자의 day-7 재방문(홈 그룹 카드가 리텐션 표면)          | analytics    |

---

## 8. 참조 문서

| 문서                                                           | 역할                                                  |
| -------------------------------------------------------------- | ----------------------------------------------------- |
| [`flutter/README.md`](flutter/README.md)                       | 빌드·시작 안내 (로드맵은 본 문서 §6.2)               |
| [`flutter/docs/HOW-IT-WORKS.md`](flutter/docs/HOW-IT-WORKS.md) | 엔진 기술 구현 SSOT                                   |
| [`flutter/docs/ARCHITECTURE.md`](flutter/docs/ARCHITECTURE.md) | 화면·패키지·인프라                                    |
| [`flutter/docs/DESIGN.md`](flutter/docs/DESIGN.md)             | 디자인 토큰 SSOT                                      |
| [`KAKAO.md`](KAKAO.md)                                         | 카톡 초대·원격 합류 동작·전제·한계                    |
